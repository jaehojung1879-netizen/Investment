# Investment Research Dashboard — v3 (paperTrading 기본)

출처가 분명하고 시점이 일치하며 사후 검증 가능한 투자 **리서치** 대시보드입니다. 종목을 많이 추천하는 사이트가 아니라, 세계적 자산운용사·매크로 전략가가 쓰는 방식(국면→위험예산→지역별 팩터 리서치→진입상태→사후검증)에 가깝게 설계했습니다.

Replay #39·#40 실패 원인과 v8 달력 보완은 [Actions 진단 문서](docs/replay-actions-39-40.md)를 참고하세요.

Replay v7의 고정 평가 달력과 성과 정의는 [v7 평가 문서](docs/replay-v7-evaluation.md),
공식 FX fixing·검증형 KR 가격 복구·corporate action 및 v9 실행 순서는
[v9 입력 복구 문서](docs/replay-v9-input-recovery.md)를 참고하세요. 입력과 계산
정의가 달라 세대 간 headline은 직접 비교할 수 없습니다. v9 실제 실행 #42·#43에서
확인된 Actions 실패와 benchmark lineage/가격 기준 수정은
[v10 Actions 복구 문서](docs/replay-v10-actions-repair.md)에 정리했습니다.

## 현재 replay / Regional Rotation 검증

현재 세대는 **`replay-v16`**입니다. v7~v15 설명과 과거 수치는 아래에 변경 이력으로
남아 있으며 현재 결과가 아닙니다. 정기 replay는 frozen input을 재현하고, 평가 anchor는
고정된 KRX/NYSE 공통 세션 달력에서 정합니다. 가격 가용성이나 실제 endDate로 이동하지 않습니다.

**Regional Rotation은 CHALLENGER**입니다. `regional-rotation-v1`은 기존 실제 기본값인
**252 달력일** lookback / temperature **0.05** / 지역 floor **0.15**를 고정합니다.
각 분기의 첫 고정 평가 anchor에서 엄격히 이전에 만기된 결과만 읽으며, 새 parameter를
역사적 최고 성과로 고르지 않습니다. Static 50/50과 Dynamic은 동일 US-only/KR-only
경로를 사용합니다. 세 포트폴리오의 비용·현금·환율·일별 위험지표 기준을 통일하고
paired bootstrap CI와 사전 정의 OFAT를 별도 연구 보고서로 만듭니다.

재현 명령·방법과 한계는 [검증 설계](docs/regional-rotation-validation-v1.md)를 참고하세요.
[실측 보고서](docs/results/regional-rotation-report.md): 155개 공통 블록에서 Dynamic−Static
CAGR +0.277%p, MDD -0.861%p(더 깊은 낙폭)이며, timing 7개 지표의 95% CI는 모두 0을 포함합니다.
낙폭 감소는 지역 분산·재선발·현금비중 변화 쪽의 설명이 우세하며, 동적 timing의 추가 가치는 불확실합니다.
역사적 검증은 prospective 검증을 대체하지 않으며 CHAMPION·production selector·Kelly·macro·
`paperTrading` 정책·`liveValidated`를 변경하지 않습니다.

### 벤치마크 상대 알파 / 현실 비용 검증

`benchmark-relative-alpha-v1`도 **research-only CHALLENGER**입니다. replay-v16의 같은 155개
고정 블록에서 비용 가정을 현실화하고, 종목의 지역 total-return benchmark 대비 만기 초과수익을
직접 수축 추정한 뒤 순알파가 양수일 때만 담는 규칙을 검증했습니다. 정규 교체는 분기 첫 anchor에서만
허용하고 그 사이에는 drifted 보유비중을 그대로 이어가며, 기존 보유종목은 즉시 매도+대체매수 비용만큼의
retention credit을 받습니다. production selector나 CHAMPION은 바꾸지 않습니다.

실측 결과는 “비용만 고치면 해결된다”는 가설을 기각합니다.

- Combined CHAMPION은 21거래일마다 평가되어 154회 리밸런싱, 평균 one-way turnover 56.10%,
  연환산 약 **6.31× NAV**를 거래했습니다. 현실 비용에서 비용 drag는 1.904%p/년에서
  **1.463%p/년**으로 낮아지지만, 비용 전에도 matched benchmark에 **−1.730%p/년** 뒤집니다.
- 기존 calibrated challenger는 비용 전 matched benchmark보다 **+0.340%p/년** 앞섰지만,
  연 4.56× 회전과 1.386%p 비용 drag 때문에 순초과수익은 **−1.046%p/년**입니다.
- 새 분기형 v1은 연 회전율을 **1.30×**, 비용 drag를 **0.423%p**까지 낮췄지만 gross selection
  수익을 잃어 순초과수익 **−1.488%p/년**이었습니다. 155개 paired block의 평균 순알파 95% CI도
  0을 포함합니다.

따라서 현재 판단은 **BENCHMARK_NOT_BEATEN**입니다. 지수보다 나은 공식이 검증됐다고 주장하지 않으며,
지금 단계의 투자 대안은 active selector보다 저비용 benchmark가 우선입니다. 다음 연구 방향은 새로운
팩터나 최적 parameter 탐색이 아니라, 기존 calibrated benchmark-relative signal을 유지하면서
교체 후보가 보유종목을 거래비용+사전 고정 문턱만큼 이길 때만 바꾸는 hysteresis 규칙입니다.
이 규칙은 새 prospective shadow 기간 전에 고정해야 하며 이번 결과로 production 승격하지 않습니다.

재현 실행은 `scripts/run_benchmark_alpha_replay.py`, CI는
`.github/workflows/benchmark-alpha.yml`, 결과는
`docs/results/benchmark-alpha-report.md`와 machine-readable JSON에 기록합니다.

### 구성 층별 기여도 분해 / selector별 selection null

`selection-value-decomposition-v1`은 **read-only DIAGNOSTIC**입니다. 위 +0.340%p가
종목 선택인지, benchmark 대비 gap이 screen의 몫인지 ranking의 몫인지는 측정된 적이
없었습니다. 같은 155개 고정 블록·같은 PIT 리서치 풀·같은 production 선택/가중 함수·같은
현실 비용에서, ranking을 한 단계씩 더 쓰는 세 rung을 결과를 보기 전에 고정해 비교합니다.
[설계와 한계](docs/selection-value-decomposition-v1.md), [실측 보고서](docs/results/selection-value-report.md).

- **CAGR gap은 분해 전까지 selection edge가 아닙니다.** calibrated challenger의
  +0.340%p는 산술 종목선택 **+0.040%p**와 복리 효과 **+0.300%p**로 갈립니다. 책의 블록
  변동성이 matched benchmark의 **0.83×**라 분산 손실이 작을 뿐이며, 88%가 저변동성
  틸트입니다. 회전율도 92~93%가 **종목 교체**이고 비중 재조정이 아닙니다.
- **"랭킹은 비용값을 못 한다"는 가설은 자기 검정에서 기각됐습니다.** 풀을 넓게 보유하면
  순초과수익 **−2.729%p**로 집중 책(−1.046%p)보다 나쁘고, 산술 선택 엣지는 ranking을 쓸수록
  단조 증가합니다(−1.771 → −1.501 → +0.040%p). 다만 rung 간 paired CI는 0을 포함합니다.
- **selection null은 selector 하나에 귀속됩니다.** sealed 리포트의 null은 CHAMPION의
  conviction score를 치환한 것이고 selector 표기가 없었습니다. 승격은 CHALLENGER를 옮기는
  일이므로 게이트가 다른 selector의 증거를 읽고 있었습니다. CHALLENGER의 null을 처음으로
  계산하면 자기 null의 **87/84/94 백분위**(p=0.134 / 0.164 / **0.065**)로 CHAMPION(61/54/91.5)보다
  훨씬 강하지만 사전 등록된 5% 기준은 **넘지 못합니다**.
- **null을 이기는 것과 benchmark를 이기는 것은 다른 기준입니다.** 여기서 "고르는 행위"는
  치환 랭킹 대비 약 **+2.9%p/년**의 가치가 있는데도 책은 여전히 matched benchmark에 뒤집니다.
  남은 적자는 ordering이 아니라 implementation입니다.

`selection_null`은 이제 모든 반환에 `selector`·`scoreSource`를 찍고, `promotionEvidence`는
`promotedSelector`를 명시하며, validator는 다른 selector의 null(또는 무표기 null)에 기댄 승격을
거부합니다. `promotionEligible`은 여전히 `False`이므로 오늘 바뀌는 동작은 없습니다.

따라서 폭(breadth)은 해법에서 배제되고, 남은 지렛대는 위에서 이미 지목한 hysteresis
규칙뿐입니다 — 이번에는 분해되지 않은 CAGR gap이 아니라 측정에 근거합니다. 이 결과로
production·CHAMPION·Kelly·macro·`paperTrading`·`liveValidated`는 바뀌지 않습니다.

### 갈아타기 문턱 / 지역별 비용

`regional-switch-hurdle-v1`도 **research-only CHALLENGER**입니다. 위 진단이 남긴 유일한
지렛대 — 보유종목을 교체할 때 **개선폭이 그 교체의 비용을 넘을 때만 바꾸는 규칙** — 을
검증했습니다. 비용은 그 교체가 실제로 지불될 지역 기준으로 매깁니다: 매수는 수수료+스프레드
절반, 매도는 거기에 증권거래세가 더해지고, 한국 왕복은 미국의 **1.6~2.5배**입니다.
[설계와 한계](docs/regional-switch-hurdle-v1.md), [실측 보고서](docs/results/switch-hurdle-report.md).

| 경로 | 총 CAGR | 비용 | 순 CAGR | matched benchmark | 순 초과 | Sharpe | MDD | 회전율 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 기존 challenger | 12.979% | 1.386%p | 11.593% | 12.639% | -1.046%p | 0.757 | -24.563% | 4.564× |
| **대조군**(문턱 없음) | 13.564% | 1.404%p | 12.161% | 12.845% | **-0.684%p** | 0.853 | -25.158% | 4.679× |
| **비용 문턱** | 15.963% | 1.133%p | 14.830% | 12.757% | **+2.072%p** | 1.057 | -22.311% | 3.567× |
| 비용+1SE | 14.867% | 0.814%p | 14.053% | 13.553% | +0.500%p | 0.848 | -35.036% | 2.814× |

대조군 대비 쌍대 차이는 비용 문턱 **+2.757%p, 95% CI [+0.122, +5.732]**로 **처음으로 0을
벗어났습니다.** 이 프로젝트에서 matched benchmark 대비 순 초과수익이 양수로 나온 첫 경로이고,
Sharpe·MDD도 함께 개선됩니다. 히스테리시스를 더 걸수록 좋아지지는 않습니다 — 1SE rung은
가장 적게 거래하면서 이득을 대부분 반납하고 낙폭은 -35.0%로 가장 깊습니다.

**사다리는 자기 대조군을 가집니다.** 이 루프는 `portfolio_replay`가 아니라 보정기를 총수익
기준으로 만들기 때문에(기존 경로는 비용차감 기준), 기존 경로와 비교하면 문턱이 하지 않은
점수 기준 변경까지 문턱의 공으로 돌아갑니다. 그래서 모든 rung은 **한 가지만 다른 대조군**과
짝지어 비교하고, 대조군-기존 경로 격차(0.362%p)는 harness 차이로 따로 공시합니다.

**두 가지는 설계 의도대로 되지 않았고, 그대로 보고합니다.**

- **비싼 시장이 더 보호되지 않았습니다.** 한국 크레딧이 미국의 3배인데 유지율은 미국이
  41.2%→88.0%, 한국이 53.2%→62.9%로 올랐습니다. 크레딧은 0이 아니라 **자리를 다투는 종목
  간 격차**에 대해 쓰이므로, 무엇이 결정하는지는 크레딧 크기가 아니라 그 지역 기대알파의
  분산과 지역 종목수 상한입니다.
- **이득이 수수료 절감이 아닙니다.** 대조군 대비 비용 drag는 0.270%p 아끼고 산술 종목선택은
  **2.157%p** 벌었습니다. 8배가 "덜 내서"가 아니라 "종목을 더 오래 들고 있어서"입니다. 설계
  이유와 작동 이유가 다른 규칙은 그만큼 약한 증거이며, 진짜 가설은 비용이 아니라 **지난달
  랭킹이 그것이 밀어내는 보유종목보다 더 노이즈가 크다**는 신호 쪽 주장입니다. 아직 미검증입니다.

**승격하지 않습니다.** `promotionEligible`은 `False`이고 prospective 증거는 없습니다. 구간
하한이 0을 0.122%p 차이로 넘었을 뿐이고, 한 번의 과거 표본에서 여러 rung을 본 뒤이며,
다중비교 보정도 없습니다. 규칙을 지금 상태로 고정하고 **앞으로 도착하는 관측으로** 판정해야 합니다.

## 실행 상태(runMode)와 데이터 모드(dataMode)

- **runMode**: `researchOnly` · `paperTrading`(기본) · `liveValidated`. 기본값은 `paperTrading`이며, **`liveValidated`는 config만으로 절대 부여되지 않습니다** — paper signal ledger에 충분한 검증 이력이 쌓여야 합니다.
- **dataMode**: `live` · `seed` · `stale` · `synthetic`. 화면에 데이터 모드와 **빌드 커밋 SHA**를 항상 표시합니다.
- README·빌드·검증 코드·화면 문구가 **동일한 안전 상태**를 표현합니다. (과거의 “README는 PAPER ONLY인데 build는 paperOnly=false” 불일치를 제거했습니다.)

화면은 `오늘의 결론 → 시장 가격·거시환경 → 포트폴리오 → 13F·국민연금 → 기회·경고 → 선정 과정 → 리서치 후보 → 검증·방법론` 순입니다. 첫 화면은 **보유 3~5종목과 각 종목의 무효화 조건**으로 시작합니다. 상세 영역은 접을 수 있지만 첫 방문에는 기본으로 펼쳐져 있으며, 기술 상태 코드는 상세에서만 보여주고 기본 화면에는 `검증 데이터 축적 중`, `Paper 운용 중`, `안전 차단` 같은 사용자용 상태를 표시합니다. 글로벌 마켓 카드와 거시 미니차트는 클릭 전 **최근 3개월**을 보여주고, 클릭하면 장기 기간을 선택할 수 있습니다. 화면의 모델 종목명은 클릭 가능하며 현재 스냅샷 PER·예상 PER·PBR·배당률·시가총액·ROE 등과 기술 참고값을 함께 보여줍니다. 결측 재무값은 추정하지 않습니다.

## 정직성 원칙

- 모든 산출물에 `schemaVersion`, `modelVersion`, `buildCommitSha`, `generatedAt`, `marketAsOf`, `sourceAsOf`, `runMode`, `dataMode`를 기록합니다(`provenance`).
- `recommendationsBlocked=true`이면 단기 아이디어뿐 아니라 **장기 picks/holdings·슬리브 비중·entryState·행동성 사유를 모두 차단**합니다. 화면에는 “데이터 검증 전으로 진입 판단을 제공하지 않습니다”라는 비행동성 상태만 표시됩니다.
- seed/synthetic/stale 데이터는 실데이터처럼 보일 수 없도록 dataMode로 명확히 구분하고, production 검증에서 non-zero exit로 실패시킵니다.
- ‘추천 비중’ 대신 **modelSleeveWeight**(완전 투자된 가상 모델 슬리브 내 비중)라고 표기합니다 — 개인 포트폴리오 추천 비중이 아닙니다.

## 장기 종목선정 엔진 v2

- KR·US를 **지역별로 독립 z-score** 산출(서로 직접 비교 금지). 지역 배분은 매크로 위험예산 레이어가 담당합니다.
- ETF·벤치마크(QQQ, SPY 등)는 개별주식 랭킹에서 제외.
- 팩터(모멘텀·밸류·퀄리티·저변동)는 **섹터 내 중립화** z-score 우선. 금융·유틸리티 등은 레버리지 페널티 예외 처리.
- `evidenceCoverage`·`dataCompleteness`·`sourceQuality`를 별도 산출해 근거가 약한 알파를 축소합니다. 이 값은 예측확률이나 통계적 신뢰도가 아니며, 실증 이력은 `empiricalValidationStatus`로 별도 표시합니다. 3개 슬리브·최소 재무 완전성 미달 시 `DATA_INSUFFICIENT`입니다.
- 리서치 슬리브는 **6~12종목 + 단일종목 상한 + 업종 상한 + 현금 하한**의 후보 목록입니다. 실제 보유는 여기서 다시 3~5종목으로 좁힙니다(아래 참조).
- 위험지표(하방변동성·최대낙폭·CVaR·베타)를 알파와 **분리 표기**. 알파와 위험·진입 페널티를 한 점수로 섞지 않습니다.
- **무효화 조건은 종목마다 다르게 계산합니다.** 상대 순위는 rank buffer의 편출선, 논리를 떠받치는 팩터는 그 팩터의 중앙값, 추세는 12-1M·200일선, 꼬리위험·최대낙폭은 **해당 지역 횡단면 분포**(90/10퍼센타일), 데이터는 슬리브·재무 커버리지 하한이 기준선입니다. 이미 넘긴 조건은 `BREACHED`로 표시합니다.

## 후보에서 보유로 — 3~5종목 집중

리서치 후보를 그대로 담으면 24종목짜리 지수 흉내가 됩니다. 포트폴리오는 별도의 선정 단계를 거칩니다.

1. **컨빅션 점수** `= (알파 백분위 − 50) ÷ 50 ÷ 하방변동성 × 근거 커버리지 × 진입상태 배율`. 알파 백분위는 횡단면 순위이므로 이 값도 **랭킹 점수이며 기대수익률·샤프비율 예측이 아닙니다**. 근거 커버리지와 진입상태 배율은 점수를 줄이기만 합니다.
2. **자격 필터** — 알파 백분위 하한(기본 66p), 진입상태·리서치 상태, 팩터 근거 유무.
3. **분산 상한** — 동일 업종 최대 2종목, 동일 지역 최대 3종목.
4. **목표 종목 수 절단** — 기본 5종목. 탈락 종목도 `selection.ranking`에 사유(`SECTOR_NAME_LIMIT`, `BELOW_TARGET_COUNT_CUTOFF` 등)와 함께 남습니다.
5. **비중** — 하방변동성 역가중 + 컨빅션 순위 0.5~1.5배 선형 틸트 → 단일종목 30%·업종 45%·테마 45%·지역 상한 적용.

현금 하한은 `max(설정값, 국면별 하한, 100 − 매크로 주식예산 상한)`입니다. 매크로 위험예산이 주식 20~45%면 포트폴리오도 45%를 넘지 않으므로, 두 층이 한 화면에서 다른 숫자를 말하는 일이 없습니다.

집중의 대가는 `concentration`으로 함께 발표합니다: **유효 종목수 = 1 ÷ 주식비중 허핀달지수**, 상위 1·3종목 비중, 그리고 종목 수 3 이하·유효 종목수 2.5 미만·1위 종목 40% 초과 시의 경고. 이 배분은 Kelly가 켜지기 전에도 작동하며, Kelly가 활성화되면 같은 보유 집합 위에 25%로 혼합됩니다.

## ‘좋은 종목’과 ‘지금 살 종목’ 분리

- `longTermResearchView`: `POSITIVE` · `NEUTRAL` · `NEGATIVE` · `DATA_INSUFFICIENT`
- `entryState`: `ACCUMULATE_GRADUALLY` · `WATCH` · `WAIT_FOR_PULLBACK` · `EVENT_RISK` · `AVOID` (추세·이격·유니버스 내 과열 백분위·변동성 급등·갭·실적 이벤트·집중도 반영)
- 회전율을 줄이는 **rank buffer**(config.longterm.rankBuffer): 신규 편입은 상위 enterPct, 기존 편입은 exitPct 아래로 내려갈 때까지 유지.
- 이전 holdings와 priorRegime은 저장소의 preview JSON이 아니라, production 검증과 Pages 배포가 모두 성공한 뒤 `signal-history:state/latest.json`에 기록된 상태만 사용합니다. 상태 없음·synthetic·버전 불일치는 빈 prior로 안전하게 동작하며 `priorState.available=false`와 이유를 artifact에 기록합니다.

## 매크로 방향·국면 엔진

**6축 진단**(성장·물가·유동성·금융여건·위험선호·이익/신용)을 표시하되, 국면 라벨은 성장×물가로 판정하고 금융여건·위험선호는 위험예산만 보정합니다. 물가는 Headline CPI(`CPIAUCSL`)·Core CPI(`CPILFESL`)·Core PCE를 나란히 수집해 식품·에너지 충격과 기조 물가를 구분합니다. Headline PPI(`PPIFIS`)와 Core PPI(`WPSFD49116`, 식품·에너지·무역서비스 제외)는 생산단계 가격 압력의 교차확인용으로 표시하되, CPI/PCE와 겹치는 신호를 이중 가중하지 않도록 국면 점수에는 직접 넣지 않습니다. 가격지수 수준이 아니라 YoY 및 3개월 연율 인플레이션율의 방향을 사용하며, 고용·실업·청구·M2/Fed assets·스프레드·VIX·커브·WTI는 지표별 변환 레지스트리를 사용합니다. 각 미니차트는 최근 3개월을 먼저 보여주고 클릭하면 1Y·3Y·5Y·10Y·MAX 장기 시계열로 열립니다. flat/상쇄 신호는 `Transition/Low confidence`입니다. confidence는 coverage·freshness·agreement로 분해하고 평가 as-of 기준으로 계산합니다.

미국 종목 추천은 구성종목 제공 순서와 무관하게 S&P 500 전체를 스크리닝합니다. 전체 목록을 확보하지 못하면 일부 알파벳 구간이나 정적 목록만으로 추천하지 않고 빌드를 실패 처리합니다. `universeSize` 제한은 시가총액순 KOSPI 스크리닝에만 적용됩니다.

팩터 계산 전 티커 순서를 canonicalize하고, 후보·Kelly 절단은 기대 초과수익→알파 백분위→근거 커버리지→데이터 완전성→하방변동성→티커 순의 명시적 tie-break를 사용합니다. 티커는 투자 판단이 아니라 마지막 재현성 기준일 뿐입니다. artifact의 `universeDiagnostics`에는 지역별 확보 수·소스·전체목록 여부·ETF 제외 수·데이터 부족 제외 수·실제 랭킹 수를 기록합니다.

월별·분기 지표에는 보수적인 고정 발표시차를 적용하지만 이는 실제 release calendar를 완전히 재현하지 않습니다. 과거 판정은 ALFRED vintage가 아닌 최신 개정 시계열을 사용할 수 있으므로 완전한 실시간 빈티지 백테스트를 주장하지 않습니다.

## 거래비용 허들 — 지역별 실측 회전율

`spreadBps`는 **full spread**입니다. 매수에서 절반, 매도에서 절반을 부담하므로 왕복 비용식은
`2 × commission + 1 × spread + sell tax`입니다. 과거 기대수익 비용식이 spread를 두 번 더해
실제 path 계산과 불일치하던 오류는 benchmark-alpha-v1에서 수정했습니다. 연구 base case는
US 5bp/side commission + 6bp full spread + 0.30bp sell levy, KR 1.5bp/side commission +
8bp full spread + 시점별 법정 sell tax를 사용합니다. 이는 1천만원 예상 거래대금의 명시적
검증 가정이며 특정 증권사의 수수료 보장이 아닙니다. matched benchmark는 비용 0으로 두어 active
알파 주장에 보수적으로 적용하고, ETF 보수는 total-return 가격에 이미 반영됩니다.

비용 추정은 후보 **한 종목**의 기대수익에서 차감되므로(`net = raw - cost`), 여기 들어가는 회전율은 책 전체가 아니라 **그 지역 슬리브의 교체율**이어야 합니다: "이 지역에 들고 있던 비중 중 리밸런스마다 몇 %를 교체하는가". 포트폴리오 수치는 현금을 포함한 책 전체로 나누므로 현금 하한이 있는 한 항상 더 작고, 실제로 리플레이 원장에서 포트폴리오 58.1%인 반면 슬리브는 US 85.3% · KR 63.1%였습니다. 하나의 숫자를 두 지역에 쓰면 **양쪽 다 과소평가**되고, 가장 심하게 도는 슬리브가 가장 적게 부담합니다. 두 슬리브가 실제로 다르다는 것은 점추정 부등호가 아니라 짝지은 차이와 구간으로 확인했습니다: 공통 리밸런스 50회에서 US−KR = **+22.20%p, 95% 부트스트랩 구간 [+9.83, +34.09]%p** (US가 더 많이 회전한 블록 34회, 동률 3회). 구간이 0을 걸쳤다면 하나의 숫자로 충분하다는 뜻이고 지역별 분리는 하지 않는 것이 맞습니다.

- 양 끝 모두 미보유인 지역은 0%가 아니라 **측정 제외**입니다. 0으로 세면 전략이 그 지역을 피한 구간이 그 지역 허들을 끌어내립니다.
- 지역별 값의 합은 포트폴리오 값과 맞지 않습니다 — 현금에는 지역이 없기 때문이며, 억지로 배분하면 책이 하지 않은 배분을 지어내는 것입니다.
- 3~5종목 책은 지역당 2~3종목이라 한 종목 교체가 33~50%p를 움직입니다. 최소 리밸런스 횟수 미만이면 포트폴리오 값으로 대체하고 `turnoverNote`에 남의 값을 쓰고 있음을 명시합니다.
- 초기 매수는 리밸런스가 아닙니다. 한 번만 내는데 사이클 수만큼 곱해지므로 평균에서 제외합니다.

`turnoverSource`가 셋을 구분합니다: `MEASURED_REPLAY_TURNOVER_REGIONAL`(지역 자체 측정) · `MEASURED_REPLAY_TURNOVER`(포트폴리오 값 대체) · `CONFIG_ASSUMPTION`(설정 가정).

## 전문가 컨센서스(반자동, 날조 없음)

`data/expert_sources.json`(기관 레지스트리)와 `data/expert_views.json`(사람이 검증한 요약)에서 **verified=true**이고 stale 하지 않은 의견만 집계합니다. 단순 평균이 아닌 **weighted median + 의견 분산**을 계산하고, 기업 IR은 독립 의견으로 취급하지 않습니다(가중치 하향). 원문 검증 전에는 내용을 만들지 않고 “검증 대기”로만 표시합니다.

## 기관 13F·국민연금 공시

`data/institutional_managers.json`에 국민연금을 포함한 운용사를 명시하고 최신·직전 **Form 13F-HR**를 SEC EDGAR에서 직접 읽습니다. 화면에는 분기말 기준일, 실제 제출일, 공시 평가액, 상위 5개 집중도, 상위 보유종목, 직전 분기 대비 주식 수 변화가 함께 표시됩니다. 종목명에서 티커를 추정하지 않으며 공시의 CUSIP·주식 수·평가액을 그대로 사용합니다. 숫자 파일명으로 정보표를 제출하는 운용사도 SEC filing index에서 찾아 처리합니다.

국민연금 전체 기금 자산배분은 기금운용본부 공식 월별 공시에서 별도로 읽습니다. 이 수치는 국내외 주식·채권·대체·단기자금을 포함한 전체 기금이고, SEC 13F는 미국 보고대상 주식 중심이므로 합치거나 동일 기준으로 비교하지 않습니다.

13F는 실시간 보유내역이 아닙니다. 통상 분기말 후 최대 45일 뒤 공개되고, 현금·공매도·비상장·일부 해외자산 및 공개 이후의 거래는 보이지 않습니다. 그래서 금액 변화는 매수·매도 판정에 쓰지 않고 **주식 수 변화**만 비교합니다. Pages workflow가 매일 08:20 KST, 평일에는 추가로 17:15 KST에 SEC 원문을 다시 확인하며 화면에 마지막 확인시각을 표시합니다. 새 제출이 없으면 같은 분기 자료가 유지됩니다. SEC/NPS 실시간 호출이 실패하면 `data/*_cache.json`의 마지막 공식 검증분을 `공식 캐시`와 원래 기준일로 표시하며 현재 자료라고 부르지 않습니다. SEC 공정접근용 연락처형 User-Agent는 `SEC_USER_AGENT`로 덮어쓸 수 있습니다. 13F 자체에는 PER·PBR·배당률이나 확정 티커가 없으므로 발행사명에서 티커를 추측하지 않습니다.

## 사후 검증(paper signal ledger)

오늘부터 생성되는 검증용 **전체 eligible cross-section**(UI 상위 15개와 분리)을 변경 불가능한 ledger에 누적합니다. ID는 date×region×ticker×modelVersion이므로 새 모델이 과거 신호를 덮어쓰지 않습니다. US는 SPY, KR은 KOSPI200 계열 벤치마크를 사용하고 동일한 종료 달력일에서 초과수익을 계산합니다. rank IC는 non-overlapping 표본의 각 date×region 횡단면에서 Spearman으로 계산한 뒤 평균·중앙값·hit ratio를 집계합니다. GitHub Actions는 **별도 `signal-history` 브랜치**를 사용해 main 재귀 빌드를 유발하지 않습니다.

`validationStatus`는 paperDays·maturedSignals·eligibleDates·지역별 IC·비용 차감 초과수익·MDD/CVaR·미달 사유를 표시합니다. Kelly 추정과 활성화는 하나의 gate를 공유하며 기본 기준은 paper 252영업일, 만기 신호 100개, HAC 유효 날짜 30개, 관측 날짜 63개입니다. 조건이 채워져도 자동으로 `liveValidated`로 승격하지 않습니다.

## 과거 Point-in-Time 재현 (Historical OOS Evidence)

**과거를 쓰지 않는 것이 정직한 것이 아니라, 과거에서 미래를 보지 않는 것이 정직한 것입니다.** 새 데이터가 1년 쌓일 때까지 기다리는 대신, 2013년부터 오늘까지의 날짜 그리드를 하루씩(기본 주 단위) 다시 살아가며 **그 시점에 실제로 알 수 있었던 정보만으로** 모델을 실행하고 판단을 동결합니다.

- **같은 모델을 재현합니다.** 팩터 정의는 `longterm.score_cross_section`, 진입상태는 `pipeline.entry`, 국면은 `pipeline.regime`을 **그대로 호출**합니다. 재현용 알파 공식을 따로 만들지 않습니다 — 그러면 아무도 운영하지 않는 모델을 검증하게 됩니다.
- **속도와 정확성을 동시에.** 종목별 시계열을 NumPy 배열로 한 번 변환한 뒤 T 시점까지의 후행 윈도로 계산합니다. 모든 지표가 후행 전용(`shift(-n)`·미래 `cummax` 없음)이므로 "잘라서 다시 계산"과 **산술적으로 동일**하며, 그 동치성은 `tests/test_historical_replay.py`가 pandas 원본 함수와 대조해 고정합니다.
- **가격**은 시점 정합적이지만 배당·액면 조정이 현재 기준이므로 `PIT_APPROXIMATE`입니다.
- **한국 재무는 DART에서 시점별로 수집합니다.** `fnlttSinglAcntAll`의 `rcept_no` 앞 8자리가 접수일자이고, 그것만이 사용을 허가합니다 — 2023년 실측으로 분기보고서는 기간말 +45일, 사업보고서는 **+71일**에 공개되므로 기간말 기준으로 쓰면 그만큼이 그대로 미래 참조입니다. 접수일자 없는 공시는 저장하지 않습니다. 원문 계정을 그대로 보관하고 TTM·주당지표·비율 산출은 별도 단계입니다(산출이 틀려도 14시간짜리 백필을 다시 받지 않기 위해). 연결/별도 구분은 레코드에 기록합니다. 호출당 약 9.4초라 120종목×2015~2025×4분기 = 5,280콜, 약 14시간이므로 `Collect DART fundamentals`가 매일 예산만큼 받아 `signal-history`에 누적합니다. DART는 2015년부터라 2013~2014년 한국 밸류·퀄리티는 비어 있고, 그 공백은 연도별로 표시됩니다.
- **재무**는 report period / filing date / **available-from date**를 구분합니다. 무료 소스에는 발표일 이력이 없으므로 현재 스냅샷을 과거에 소급 적용하지 않고 **밸류·퀄리티 슬리브를 제외**합니다(`CURRENT_SNAPSHOT_ONLY`). 팩터 커버리지가 떨어지면 알파는 그만큼 수축합니다.
- **매크로**는 고정 발표시차로 "그날 공개됐는가"는 막지만 개정 이전 값(ALFRED vintage)은 확보하지 못해 `REVISED_HISTORY`입니다. 확보하지 못한 이유는 벤더가 아니라 설정입니다 — `datafeed.fetch_macro_vintages`와 `pit_data.MacroVintageView`가 이미 붙어 있고 `historicalReplay.macroVintageSeries`가 비어 있을 뿐입니다.
- **그런데 전부 켠다고 `PIT_EXACT`가 되지는 않았습니다 — 파생 컬럼 때문에 구조적으로 불가능했습니다.** `Yield_Curve`는 10Y−2Y로 *계산되는* 컬럼이라 FRED series id가 없고, ALFRED가 그 이름으로 답할 방법이 없습니다. 집계 `pit_status`는 패널 컬럼에서 빈티지 확보 시리즈를 빼서 남는 것이 있으면 `PIT_APPROXIMATE`를 반환했으므로, 몇 개를 켜든 이 컬럼이 항상 남아 `vintageMacro` 게이트는 데이터와 무관하게 열릴 수 없었습니다. 두 빈티지 시리즈의 차이는 그 둘만큼 빈티지이므로, 면제가 아니라 **빈티지 입력으로 재계산**합니다 (`pit_data.DERIVED_MACRO_COLUMNS`). 파생 정의는 한 곳에만 있고 `datafeed`·`make_seed`가 그것을 씁니다 — 재현이 프로덕션 패널과 다른 산술을 조용히 쓰는 경로를 닫기 위해서입니다.
- **켜기 전에 시리즈별로 재는 것이 먼저입니다.** `vintage_column`은 as-of 시점까지 발표된 프린트만 남기므로, ALFRED 이력이 2019년에 시작하는 시리즈를 켜면 2013년 재현 날짜에서 그 컬럼은 통째로 NaN이 되고 `pit_status`는 오히려 `PIT_EXACT`로 올라갑니다 — 개정된 숫자를 숫자 없음과 바꾸고 더 좋은 라벨을 받는 것입니다. `Probes` 워크플로(`probe: alfred-macro-vintages`)가 패널 전 컬럼에 대해 리플레이 시작일 기준 실제 서빙 가능 기간 수를 재고, 하나라도 `VINTAGE_TRUNCATED`면 켜지 않습니다.
- **유니버스**는 과거 구성종목 파일이 있으면 상장·상장폐지를 반영하고, 없으면 모든 관측치에 `SURVIVORSHIP_BIAS_UNRESOLVED`를 기록하고 증거 가중치를 낮춥니다.
- **생존편향 결손은 두 가지이고 따로 셉니다.** `constituentCoveragePct`는 “편입이력이 있다고 아는 종목 중 가격을 확보한 비율”, `membershipCoveragePct`는 “단면 중 편입이력을 아는 비율”입니다. 미국 구성종목 파일만 있으면 아는 종목은 모두 가격이 있으니 앞의 값은 100%가 되고, 한국 단면 전체는 편입이력 미상인 채로 남습니다. 그래서 발표되는 `affectedObservationsPct`는 두 결손의 **합집합**(가격 결측 ∪ 편입이력 미상)이며, 승격 게이트와 full-fidelity 주장은 두 값이 **모두** 100%일 때만 통과합니다. 한쪽만 측정된 상태는 통과가 아니라 `미산정`입니다.
- **한국 편입이력을 메울지는 가격 커버리지가 먼저 결정합니다.** 편입이력만 확보하면 과거 편출 종목은 `편입이력 확인 · 가격 없음`이 되어 단면에서 그대로 탈락하므로, KR unvouched는 100%에서 약 40%까지만 내려가고 멈춥니다. 그 아래로 가려면 상장폐지 종목의 가격이 필요한데 리플레이 패널이 쓰는 Yahoo는 이를 거의 서빙하지 않습니다(첫 실행 측정 3,010개 중 17개). FinanceDataReader의 `exchange='KRX-DELISTING'`가 남은 무료 후보이고, `scripts/measure_kr_delisting_coverage.py`가 이를 측정합니다 — 리플레이 자신의 기준(폐지일 이전 273세션)으로, 폐지연도별 층화표본과 Wilson 구간으로. 분모는 3,010이 아니라 **리플레이 시작 이후 폐지된 종목**입니다: 그 전에 사라진 이름은 리플레이가 만드는 어떤 단면에도 없었으므로 결손이 아닙니다. 네트워크 작업이라 `Probes` 워크플로(`probe: kr-delisting-coverage`)에서 수동 실행합니다.
- **측정 결과 (2026-08-30, 표본 150, 리플레이 시작 이후 폐지분):** 리플레이가 스크리닝하는 **KOSPI 기준 34.55%** (19/55, 95% CI [23.36, 47.75]) — 구간 전체가 50% 아래입니다. Yahoo는 0.67%(1/150)로 사실상 없음이 재확인됐습니다. 시장을 합치면 53.33%가 나오지만 표본 150개 중 95개가 KOSDAQ·KONEX이고 리플레이는 그 목록을 랭킹하지 않습니다 — 합친 숫자는 결정 질문에 **반대 부호로** 답합니다. 그래서 헤드라인은 `--market KOSPI`(기본값)로 고정돼 있습니다.
- **그래서 KOSPI 200 편입이력 작업은 착수하지 않습니다.** 편입이력을 완벽히 만들어도 복원된 과거 편출 종목의 약 2/3는 가격이 없어 단면에서 다시 탈락합니다. 남는 불확실성은 양방향입니다: KOSPI 폐지종목에는 애초에 투자 유니버스(시총순 상위)에 든 적 없는 소형주가 다수라 실제로 필요한 대형주만 보면 커버리지가 더 높을 수도, 더 낮을 수도 있습니다. 폐지종목의 과거 시가총액이 없어 그 분해는 하지 못했고, 하지 않은 채로 유리한 쪽을 고르지 않았습니다. 현재 상태는 결손이 **측정되어 HIGH로 발표되는** 것이며, 그것이 지금 정직한 상태입니다.
- **결손은 지역과 연도로 나눠 발표합니다.** 위험 등급은 평균이 아니라 **최악 지역** 기준입니다 — 512개의 미국 종목이 119개의 한국 종목을 덮으면 “81% 확인”이라는 부분 결손처럼 읽히지만 실제로는 한 지역 전체가 미상입니다. 시간축도 같습니다: 2013년 S&P 500은 벤더에 묻기 전부터 500종목 중 219종목이 빠져 있고 2025년은 34종목이라, 10년 평균 한 숫자는 그 안의 어떤 단면도 설명하지 않습니다. `universeCoverageByRegion`·`universeCoverageByYear`와 `worstCoveredRegion`·`worstCoveredYear`가 artifact와 운영 로그에 함께 남습니다.
- 관측치마다 `pitCoverage`·`pitQuality`·`lookAheadCheckPassed`·`survivorshipRisk`를 남기고, PIT 품질이 낮은 관측치는 **Kelly calibration에서 제외**합니다. 다만 커버리지는 *실제로 사용된* 소스에 대해서만 계산합니다 — 쓰지 않은 슬리브의 부재는 leakage가 아니라 커버리지 문제이고, 그 페널티는 `evidenceCoverage`와 증거 가중치에서 별도로 부과합니다.
- **ledger는 완전히 분리**합니다: `ledger/historical/<replayVersion>/`(HISTORICAL_OOS) vs `signals.jsonl`·`outcomes.jsonl`(PROSPECTIVE_PAPER). 두 증거는 절대 한 파일·한 표에 섞이지 않습니다.
- **historical ledger는 월 단위로 쪼개 gzip으로 저장**합니다(`signals-YYYY-MM.jsonl.gz`). 10년치 주간 단면은 단일 JSONL로 쓰면 900MB라 GitHub의 100MB blob 제한에 걸려 push 자체가 거부됩니다 — 실제로 replay 잡이 이틀 연속 계산을 마치고 전량 유실했습니다. 샤드는 결정적으로 직렬화되므로(gzip mtime 고정·행 정렬) 내용이 안 바뀐 샤드는 git이 변경으로 보지 않고, 매일 커밋되는 양은 새로 생긴 샤드뿐입니다. 크기 한계는 push 전에 `historical_store.assert_pushable`이 먼저 잡습니다.
- 기록은 **append-only**이며 `modelVersion`·`replayVersion`·`featureVersion`·`dataVersion`을 함께 남깁니다. 모델을 바꾸면 새 세대가 별도로 쌓이고 기존 기록은 보존됩니다. CI는 **증분 실행**이라 이미 있는 날짜는 다시 계산하지 않습니다.
- 재현에서 빠진 것도 숨기지 않습니다: 단기 LightGBM 점수는 비용 문제로 재현하지 않고 `null`, 실적 캘린더가 없어 `EVENT_RISK` 분기 일부가 비활성, 섹터 로테이션은 ETF RRG가 아닌 구성종목 상대강도 프록시입니다.

### Phase 현황 — 각 Phase는 단독으로 테스트 가능합니다

| Phase | 내용 | 상태 | 무엇이 막고 있나 |
|---|---|---|---|
| **1. Replay Infrastructure** | time-safe replay engine, price-only PIT, historical signal ledger, outcomes, leakage 탐지 | **구현 완료 · 벤더 결정성 복구 중** | 2026-08-20·23·24에 동일 커밋이 KR 벤치마크 커버리지 452/452와 0/452를 번갈아 산출했습니다. 원인은 코드가 아니라 `^KS200`의 간헐적 벤더 결측이며, outcomes가 매 실행 재계산되기 때문에 하룻밤 결측이 증거 기반 전체를 다시 씁니다. `pipeline/benchmark_source.py`의 벤더 이중화 + 커밋된 스냅샷이 이 경로를 닫습니다. |
| **2. PIT Fundamentals / Macro** | publication-aware 재무, vintage-aware 매크로, historical universe | **인터페이스 완료 · 데이터 대기** | 무료 소스에 발표일 이력·ALFRED vintage·과거 구성종목이 없음. `pit_data.FundamentalStore.from_jsonl` / `UniverseHistory.from_json`에 파일만 넣으면 즉시 활성화되고 PIT 커버리지가 올라갑니다. |
| **3. Historical Calibration** | alpha bucket 기대수익 + 만기 인지형 상승확률·상하방 분포 calibration, historical Kelly prior, shrinkage | **구현 완료 · 재현 세대 재생성/데이터 gate 대기** | 새 세대 재현 후에도 지역별 벤치마크 커버리지, Brier·ECE·log-loss와 과거 구성종목·PIT 재무·필요 시 vintage 거시 gate를 모두 통과해야 Kelly가 켜집니다 |
| **4. ML Opportunity / Warning** | change feature dataset, walk-forward ML, calibrated probability, radar | **완료 · acceptance gate 대기** | gate 통과 전까지 규칙 기반 점수 사용 (설계된 동작) |
| **5. Prospective Bayesian Update** | historical + live posterior, drift 감지, Kelly 영향도 적응 | **완료 · 가동** | prospective ledger가 쌓이면 자동으로 비중이 이동 |

> **현재 상태 (2026-09-04).** `replay-v6`을 방금 열었기 때문에 현 세대 기록이 0건입니다. 이번에는
> 실패가 아니라 **의도된 전환**입니다: DART 수집으로 한국 116종목(나머지 3종목은 고유 corp_code가 없는
> 우선주)에 PIT 재무가 들어오면서, v5까지 가격 팩터만으로 매겨지던 KR 단면에 밸류·퀄리티가 처음 반영됩니다.
> `alphaPercentile`은 그날 단면 안의 순위이므로 2개 슬리브로 매긴 순위와 4개로 매긴 순위는 같은 측정이
> 아니고, 그래서 v5의 379,873 신호·372,883 결과는 덮어쓰지 않고 자기 세대로 보존됩니다(이전 네 세대 합계
> 1,679,326건). 일일 재현이 v6을 다시 채울 때까지 과거 검증 패널은 비어 있으며, 그동안
> `historicalValidation.pipelineHealth`는 `historical_replay_generation_not_produced`로 표시됩니다 —
> 세대를 새로 여는 값이지, 파이프라인 고장이 아닙니다.

Phase 2가 비어 있어도 Phase 1·3·5는 정상 동작합니다. price-only 재현의 알파는 모멘텀·저변동 슬리브만으로 만들어지고, 빠진 밸류·퀄리티는 `evidenceCoverage` 축소와 증거 가중치 할인으로 이미 반영됩니다.

## 최종 3~5종목 Historical Portfolio Replay

개별 신호 성과와 별도로, 각 과거 리밸런싱 날짜에 실제 사용 가능했던 현 세대 재현 신호를 모아 `research candidate → entry state → candidate filter → concentrated selection → baseline weight → macro cash floor → final historical weight`를 다시 실행합니다. 라이브와 replay는 `selection_and_baseline`이라는 같은 순수 함수를 호출하므로 종목·업종·지역 상한과 기준 비중 공식이 따로 복사되지 않습니다. 날짜마다 선택 종목, 근접 탈락 사유, 팩터/PIT 커버리지, 컨빅션 순위, 현금, 국면, 비용 가정과 버전을 감사 표본에 남깁니다. 일별 바의 시장 달력 날짜를 먼저 정규화하며, 126일 지역별 벤치마크 커버리지가 95% 미만이면 초과수익 검증 원장을 게시하지 않습니다. 벤치마크 시계열 자체는 벤더 이중화 → 세션 달력 타당성 검사 → 커밋된 스냅샷 순서로 확보하며(`pipeline/benchmark_source.py`), 스냅샷으로 재현한 지역은 `SNAPSHOT_FALLBACK`으로 표시됩니다.

- Production champion은 계속 `ALPHA_RANK_PER_DOWNSIDE_RISK`입니다. `CALIBRATED_EXPECTED_RETURN_PER_DOWNSIDE_RISK` challenger는 같은 날짜·후보·제약·비용에서만 비교하고, T일에는 `outcomeEndDate <= T`인 결과만 expanding calibration에 넣습니다.
- **무작위 순위 대비 검정.** 벤치마크 대비 초과수익은 유니버스 캐리와 제약·비중 규칙까지 함께 담고 있어서 "종목을 고른 것이 값어치를 했는가"에 답하지 못합니다. 그래서 동일 날짜·동일 후보군·동일 상한·동일 진입배율·동일 현금하한·동일 비중함수·동일 거래비용을 그대로 두고 **컨빅션 점수만 종목 사이에서 무작위로 재배치**한 분포를 만들어 비교합니다(`pipeline/selection_null.py`). 실제 점수를 섞으므로(노이즈 생성이 아님) 귀무 포트폴리오의 집중도·틸트 형태가 실제와 같습니다. 알파 선정 하한(`minAlphaPercentile`)은 그 자체가 점수 판단이므로 귀무에서는 해제하고, 데이터·진입상태 배제는 그대로 유지합니다. `BEATS_RANDOM` 판정은 **위험조정 지표(IR·Sharpe)** 를 통과해야 하며, 초과수익만 이기고 IR이 분포 안에 있으면 "위험을 더 져서 얻은 수익"이므로 우위로 보지 않습니다. 이 검정은 이미 alpha로 걸러진 후보군 **안에서의 순위·집중 단계**만 검증하며, 연구 스크리닝 자체는 측정하지 않습니다.
- **동일 블록 쌍대 비교.** champion과 challenger는 데이터 가용성과 독립적인 **하나의 고정 블록 스케줄**로 비교하고, 점추정 부등호가 아니라 부트스트랩 95% 구간이 붙은 쌍대 차이로 판정합니다. 서로 다른 기간(2013-01 vs 2013-11)과 서로 다른 벤치마크 구성으로 비교하던 이전 방식은 selector 차이가 아니라 기간 차이를 재고 있었습니다.
- **완전성 하한.** 구성종목 중 하나라도 만기 벤치마크가 없으면 그 포트폴리오는 통째로 제외되는데, 제외되는 종목은 거래정지·상장폐지 쪽에 몰립니다. 따라서 측정 가능했던 것만 모아 만든 경로는 다른 전략입니다. 블록 통계에는 `minPortfolioCompletenessPct`를 적용하지만, 연속 NAV 헤드라인은 성숙 블록 100%가 필요하며, 남은 비중을 재정규화해 대신 싣지 않습니다.
- 주간의 겹치는 21/63/126/252일 forward return을 CAGR이나 MDD로 이어 붙이지 않습니다. 자산곡선·CAGR·Sharpe·Sortino·MDD·CVaR·회전율은 실제 비용을 적용한 **겹치지 않는 리밸런싱 블록**으로 별도 구성합니다. 모든 날짜 평균은 이 경로 지표와 구분합니다.
- PIT 재무는 `replay-v6`부터 한국 116종목에 들어왔고 미국은 아직 없습니다. **FMP는 열려 있습니다 — "무료 티어에서 재무제표를 뺐다"는 제 서술을 철회합니다(2026-09-05).** 같은 실행에서 `/stable/income-statement`가 AAPL에 **HTTP 200**으로 실제 분기 재무를 돌려줬습니다(`date 2026-06-27`, `cik 0000320193`, **`filingDate 2026-07-31`**, `acceptedDate` 포함). 판정 `STATEMENTS_AVAILABLE`, 키도 살아 있습니다(`profile`·`quote` 둘 다 200). 그런데 같은 실행의 종목 루프는 15회 전부 402였습니다. 두 호출의 차이는 **`limit` 하나뿐**입니다 — 서빙된 쪽은 `limit=4`, 거절된 쪽은 `limit=400`. 즉 402는 "재무제표를 안 준다"가 아니라 **"한 번에 그만큼의 과거는 안 준다"**이고, 상한은 추론할 게 아니라 재야 할 숫자입니다. `find_limit_cap()`이 4→400 사다리를 올라가며 처음 거절당하는 칸에서 멈추고(FMP 상한은 단조라 그 위를 더 사봐야 쿼터만 씁니다) 벤더의 거절 문장을 그대로 싣습니다. 표본은 그렇게 **실측된 상한으로** 받습니다. 덧붙여 필드명 함정도 하나 있었습니다: `/api/v3`는 오탈자인 `fillingDate`를 썼고 `/stable`은 `filingDate`로 고쳤는데, 프로브는 v3 철자만 알고 있었습니다. 그대로였다면 **모든 행이 제출일을 갖고 있는 응답을 두고 "제출일 0건, PIT 불가"라고 보고**했을 것입니다. 이제 두 철자를 다 받고 어느 쪽이 답했는지를 기록합니다. **SEC의 GitHub Actions IP 거절도 이제 실측입니다(2026-09-04).** 한때 "헤더 때문일 수 있다"고 정정했었는데, 그 정정을 다시 거둡니다 — 가설은 틀렸고 원래 결론이 맞았습니다. 다만 그때는 가정이었고 지금은 증거가 있습니다. `Probe SEC header isolation`이 같은 실행·같은 러너·같은 분에 헤더 세트 4종 × 호스트 2곳 = 8회를 던졌고 **8회 전부 403**, 그것도 **바이트 수까지 동일한**(data.sec.gov 4,819B, www.sec.gov 1,925B) 같은 차단 페이지였습니다. `Accept-Encoding: gzip, deflate`를 추가해도, UA를 SEC 문서 형식으로 바꿔도, 정책이 명시한 최소 세트만 보내도 응답이 한 바이트도 달라지지 않습니다 — SEC가 우리 헤더를 **읽지도 않는다**는 뜻이고, 거절은 요청 모양이 아니라 출발지에서 일어납니다. 판정은 `REFUSED_ON_EVERY_HEADER_SET`입니다.
- **SEC는 2026-08-15에 우리에게 응답했고, 그 뒤 주소 단위로 막혔습니다 — 세 축을 다 재고 확정합니다(2026-09-04).** `data/institutional_13f_cache.json`이 `sourceMode: LIVE_SEC`, `fetchedAt: 2026-08-15T23:00:24Z`이고 출처는 지금 거절당하는 `data.sec.gov`입니다. 그래서 ① 요청 모양(헤더 4종) ② 정책 준수(`Accept-Encoding`·문서형 UA) ③ **송신 주소**를 각각 흔들어봤고, 마지막이 남아 있던 축이었습니다. 8개 병렬 러너가 **서로 다른 8개 주소**를 받았고 (`4.154.40.4`, `4.154.135.147`, `4.155.255.60`, `20.109.39.56`, `20.168.110.19`, `20.169.93.176`, `128.24.162.210`, `172.214.155.183` — 전부 Azure 대역) **16회 요청 전부 403**입니다. 판정 `REFUSED_ON_EVERY_ADDRESS`. 주소 추첨이 아니라 대역 차단이고, 8월 이후 생긴 것입니다.
- **따라서 미국 PIT 재무는 SEC 말고 다른 벤더가 필요합니다 — 그리고 그 벤더는 이미 응답하고 있습니다.** SEC는 키 제도가 없으므로 GitHub Actions에서 나가는 한 닫혀 있고, 그 자리를 FMP가 받습니다. 위 실측대로 `/stable` 재무제표가 200으로 서빙되고 `filingDate`까지 실려 옵니다 — 제출일은 DART의 접수일, SEC의 `filed`와 같은 역할이므로 미국 PIT의 필수 조건이 갖춰졌다는 뜻입니다. 남은 것은 **한 번에 받을 수 있는 과거의 길이**이고 그것이 `limitCap`으로 측정됩니다. Polygon.io·SimFin은 이제 대안이지 다음 순서가 아닙니다.
- **그런데 FMP가 응답한다는 것과 미국 경로가 열렸다는 것은 아직 같은 말이 아닙니다 — 실측된 상한이 리플레이 시작보다 11년 뒤에 있습니다.** run #4(2026-09-05)에서 `limit=20`은 FMP 자신의 문장으로 거절됐고(`The values for 'limit' must be between 0 and 5 based on your current subscription`), 서빙된 어떤 호출도 **2025-09-26**보다 과거로 가지 못했습니다. 리플레이는 **2013-01-01**에 시작합니다. 게다가 `/stable` 재무제표 엔드포인트는 날짜 인자를 아예 받지 않아 물어볼 창도, 넘길 오프셋도 없고, 같은 실행에서 `O`는 종목 자체가 구독 밖이라며 402였습니다. 그러므로 **오늘 기준 미국 PIT 재무로 CI에서 증명된 경로는 없습니다.** 여기서 다른 벤더를 이름만 대는 것이 바로 run #49(검증 안 된 KRX 엔드포인트, 119종목 전부 400)를 만든 동작이라, 대신 `Probe US PIT fundamentals sources`가 넷을 하나의 기준으로 잽니다 — 도달성은 **키 없이** 묻고(구조화된 거절은 응답으로 세되 HTML 차단 페이지는 아니고, 아무것도 오지 않은 것은 아직 벤더 탓이 아닙니다), 깊이는 **날짜로** 묻고(창을 받고도 최근 행만 오면 깊이가 아니라 v12에서 한국 46,356행을 지운 Naver `fchart` 모양입니다), 공시일을 깊이보다 먼저 판정하고, 살아 있는 이름과 상장폐지된 이름을 **절대 합산하지 않습니다**(미국 829개 중 219개가 2013년 이전 멤버였다가 사라졌고, 합치면 생존 편향이 커버리지 73%로 읽힙니다). SEC도 두 호스트에서만 판정났던 터라 `efts.sec.gov`와 www 없는 `sec.gov`를 이미 거절당하는 두 호스트를 baseline으로 같은 실행에 넣어 함께 묻습니다. 자세한 내용은 `docs/us-pit-fundamentals-source.md`.
- **미국 PIT 소스 프로브 run #1 실측(2026-09-13): SEC는 네 호스트 전부 닫혔고, SEC가 아닌 벤더는 넷 다 우리에게 응답합니다.** 차단 페이지의 **바이트 수가 짝을 이룹니다** — `efts.sec.gov`가 `data.sec.gov`와 같은 4,819B, www 없는 `sec.gov`가 `www.sec.gov`와 같은 1,925B. 네 개의 독립된 거절이 아니라 **두 종류의 차단 페이지가 네 호스트에 걸친 것**이고, 즉 호스트별 정책이 아니라 도메인 정책입니다. 이미 거절당하던 두 호스트가 같은 실행에서 그대로 거절당했으므로 그날 운도 아닙니다(`REFUSED_ON_EVERY_SEC_HOST`). 반대로 `finnhub`·`polygon`·`simfin`·`alphavantage`는 **키 없이 물었을 때 전부 자기 프로토콜로 응답했습니다** — `HOST_REFUSED`도 `NO_ANSWER_FROM_HOST`도 0건. SEC 차단이 GitHub Actions 주소 대역 전반의 문제가 아니라 **sec.gov만의 정책**이라는 것도 이걸로 함께 증명됐습니다. 덧붙여 alphavantage는 **HTTP 200에 에러 본문**을 실어 보냈습니다 — 상태코드로 판정했다면 "데이터가 왔다"로 읽혔을 자리이고, 본문이 판정하게 한 설계가 첫 실전에서 값을 했습니다. FMP를 날짜로 다시 물은 결과는 `TOO_SHALLOW`: 가장 이른 기간 2025-06-27~06-30, 5분기가 전부인데 리플레이는 54분기가 필요합니다.
- **그리고 그 실행이 제 코호트 선정의 결함을 드러냈습니다 — 정정합니다(2026-09-13).** FMP가 떠난 이름 네 개(`AA, ABC, ACE, AET`)를 전부 `Special Endpoint : This value set for 'symbol' is not available under your current subscription`으로 거절했는데, 저는 이걸 "사라진 이름을 못 준다"로 읽을 뻔했습니다. **`AA`(Alcoa)는 지금도 거래되는 티커입니다.** `universe-history.json`의 `delisted`는 상장폐지일이 아니라 **스크리닝 유니버스에서 빠진 날**이고, 알파벳 순 선정이 하필 아직 살아 있는 이름을 코호트 맨 앞에 세웠습니다. 살아 있는 대형주까지 같은 문장으로 거절당했다는 건 이게 **구독 티어의 종목 제한**이라는 뜻이고, 돈으로 풀릴 수 있는 문제이지 생존 편향이 아닙니다. 그래서 두 가지를 고쳤습니다: ① `LIVING_ONLY`를 둘로 쪼개 **거절**은 `DEPARTED_REFUSED`(벤더 문장을 판정에 싣습니다), **빈 응답**만 `LIVING_ONLY`(답할 의사가 있었는데 가진 게 없었다 = 진짜 커버리지 구멍)로 봅니다. ② 떠난 표본을 알파벳 순이 아니라 **가장 먼저 떠난 순**으로 고릅니다 — `ANR, BIG, CBE, DV`, 리플레이의 첫 단면이 실제로 들고 있었고 대부분 진짜 은퇴한 티커입니다.
- **Probes run #2: polygon 파서 가설이 맞았고, 판정은 또 틀렸습니다(2026-09-13).** 정규화 키를 후보에 넣자 polygon도 **7개 팩터 전부 계산 가능**이 됐고 `fieldsSeen`이 이유를 말해줍니다 — `accounts_payable`·`assets`·`basic_average_shares`·`cost_of_revenue`, 제출자 태그가 아니라 정규화된 snake_case였습니다. (finnhub 쪽 `fieldsSeen`은 반대로 `AccountsPayableCurrent`·`AdditionalPaidInCapital` — 제출자 원본 태그를 그대로 재서빙합니다.) **그런데 polygon이 `LIVING_ONLY`(생존 편향)로 나왔는데, 떠난 이름 넷 중 셋이 빈 응답이 아니라 `You've exceeded the maximum requests per minute` 였습니다.** 그 셋(`BIG`·`CBE`·`DV`)은 바로 한 실행 전에 3~6개 기간씩 서빙됐던 이름입니다 — polygon은 갖고 있습니다. 프로브가 잰 것은 **자기 자신의 호출 간격**이고 그걸 벤더의 커버리지로 적었습니다. "천천히 하라"는 거절은 데이터에 대한 관측이 아니라 **질문을 하지 못했다**는 뜻이라, `RATE_LIMITED`를 별도 결과로 분리하고 모든 실제 답보다 낮은 순위에 두었으며, 코호트에 하나라도 섞이면 커버리지 판정 대신 `RATE_LIMITED_BEFORE_MEASURED`입니다. simfin은 `TOO_SHALLOW`인데 이번엔 **대조 창이 서빙됐고** 그게 "과거가 없다"와 "질문을 못 알아들었다"를 가릅니다 — 다만 대조 결과가 로그에 찍히지 않아 판정의 근거가 보이지 않았고, 이제 찍습니다.
- **미국 수집기를 넣었습니다 — 다만 파생(derive)은 일부러 아직 쓰지 않았습니다(2026-09-13).** `scripts/collect_finnhub_fundamentals.py`가 `Collect fundamentals` 워크플로의 `us` 잡으로 돌면서 `ledger/fundamentals/us/finnhub-YYYY.jsonl.gz`를 `signal-history`에 씁니다 — 한국 수집기와 같은 골격(예산·재개·바이트 결정적 샤드). 유니버스는 **한때 멤버였던 미국 829종목 전부**이고(그중 219개가 리플레이 시작 전 멤버였다가 떠났습니다), 창은 2012-01-01부터 겹침 없이 10개, **`filedDate` 없는 공시는 저장하지 않으며**, 계정과목은 제출자 태그 그대로 둡니다. 속도 제한을 받으면 그 창을 완료로 적지 않고 멈춥니다. **파생이 없는 이유:** `dart_derive`는 수집기의 필드 인벤토리가 2,927건에서 "Q3 손익이 3개월인가 9개월인가"를 **실측한 뒤에야** 쓸 수 있었고, 누적 현금흐름을 분기로 읽었다면 FCF가 네 배로 부풀면서 아무 에러도 안 났을 것입니다. 미국도 같은 질문이 열려 있습니다 — 10-Q는 3개월 컨텍스트와 누계 컨텍스트를 둘 다 제출하고, 벤더가 `report.ic`에 어느 쪽을 담는지는 **벤더에 대한 사실**입니다. 그래서 이번 수집은 **form 타입별 기간 길이 분포**를 재서 `inventory.json`에 적습니다(합산 카운트는 TTM이 필요로 하는 질문에 답할 수 없습니다). 파생은 그 답에 맞춰 별도로 쓰고, 그때까지 리플레이에는 아무것도 연결하지 않습니다 — `build_pit_fundamentals.py`는 여전히 한국 스토어만 알고 `replay.yml`도 `pit-kr.jsonl`만 넘깁니다.
- **미국 PIT 경로가 열렸습니다 — `finnhub`, 키를 넣은 첫 실행에서 실측(2026-09-13).** `Probes`(`us-pit-fundamentals`) 실행에서 살아 있는 네 이름(AAPL·JPM·XOM·KO)이 2012-01-01~2013-06-30 창 안에 각 5개 기간을, 유니버스를 떠난 이름도 ANR 7개·BIG 4개·CBE 3개를 **공시일(`filedDate`)과 함께** 돌려줬습니다(DV만 결손). 결정적으로 **실전 value·quality 7개 팩터가 전부 계산 가능**합니다 — 미국에서 13년간 한 번도 검증되지 않았던 value 0.3 + quality 0.2가 이걸로 닫힙니다. 전체 백필은 종목당 10회 × 829종목 = **8,290회**. **그런데 같은 실행이 이 프로브의 결함 셋을 드러냈고, 셋 다 증거가 받쳐주지 않는 단정을 만들어냈습니다.** ① polygon을 `OPEN`으로 불렀는데 같은 리포트에 **아홉 개 계정과목을 하나도 못 찾았다**고 적혀 있었습니다 — 깊이와 공시일은 PIT의 조건이지 사용 가능의 조건이 아닙니다. 이제 계산 가능한 팩터가 0이면 `ACCOUNTS_NOT_FOUND`입니다. 원인은 polygon이 아니라 제 파서일 가능성이 높습니다: 정규화해서 주는 벤더에게 제출자 원본 US-GAAP 태그(`NetIncomeLoss`)를 찾았습니다 — `fillingDate` 교훈을 반대편에서 다시 밟은 것입니다. ② 못 찾은 이름만 보고하고 벤더가 **실제로 보낸 이름**은 안 보고했습니다. 이제 `fieldsSeen`으로 싣습니다 — "netIncome 없음"이 "`net_income_loss`로 온다"가 되면 후보에 넣을 이름이지 은퇴시킬 벤더가 아닙니다. ③ 빈 응답에 대조가 없었습니다. simfin이 **AAPL 포함 8개 표본 전부 0행**을 돌려줬는데 `TOO_SHALLOW`로 적혔습니다 — AAPL이 그 창에 공시를 안 했을 리 없으니 용의자는 우리 쿼리입니다. 이제 최근 창(2025-01-01~2026-06-30)을 대조로 던지고, 거기도 비면 `REQUEST_NOT_RULED_OUT`입니다. polygon과 simfin은 실패가 아니라 **미해결**이고, 재측정 전까지 백업으로 쓰지 않습니다.
- **첫 수집이 4,971건을 가져왔고 거절 0건 — 그리고 같은 실행이 파생을 막고 있던 네 가지를 드러냈습니다(2026-09-13).** 1,500창을 사서 2011~2015 샤드를 채웠고, **모든 공시가 `filedDate`를 달고 왔습니다**(이 수집이 존재하는 이유인 그 필드입니다). ① **기간은 누계로 보입니다 — 다만 그건 아직 증거지 증명이 아닙니다.** 기간 길이가 분기 1,801 · 반기 1,615 · 3분기 1,553으로 갈렸는데, 독립된 세 분기라면 반기·3분기 버킷 자체가 생기지 않습니다. 그러나 `report.ic` 항목들은 자기 날짜가 없으므로 이건 **봉투에 대한 사실**입니다. DART가 같은 질문을 필드 이름이 아니라 **84개 기업의 값 비율**로 끝냈고, 이미 산 데이터로 같은 방법을 씁니다 — 한 티커-연도 안에서 반기/1분기와 3분기/1분기, 누계라면 ≈2.0·≈3.0, 독립 분기라면 ≈1.0·≈1.0, 통계는 **기업 간 중앙값**입니다(고르게 버는 회사는 없고, 계절성 있는 한 기업은 어느 쪽 해석이든 반증하는 것처럼 보입니다). 네 개 흐름 계정을 따로 재고, 넷이 갈리면 평균내지 않고 **판정을 보류합니다**. 비율 30개 미만이거나 중앙값이 두 밴드 사이에 있으면 `INCONCLUSIVE`로 **0이 아닌 코드로 끝납니다** — 초록 체크가 데이터가 주지 않은 답을 암시하지 않도록. ② **rollforward의 `FY` 항을 애초에 수집하지 않고 있었습니다.** `TTM(Y,Q) = FY(Y-1) − cum(Y-1,Q) + cum(Y,Q)`에서 수집기는 `freq=quarterly`만 물었고, 연간 공시가 없으면 TTM이 **덜 정확한 게 아니라 아예 불가능**합니다. 이제 연간을 먼저 돌립니다(연 1건, 싼 쪽이자 나머지가 걸리는 앵커). 이미 산 1,500창은 **다시 사지 않습니다** — 세 항목짜리 기록은 그것이 실제로 했던 일, 즉 분기 패스를 뜻합니다. ③ **모든 GAAP 태그가 두 벌로 옵니다** — `Assets`와 `us-gaap_Assets`가 같은 계정입니다. 한 철자만 찾는 파생은 자기 커버리지를 조용히 반토막냅니다. 다만 **알려진 네임스페이스만** 떼어냅니다: 제출자 고유 태그에도 밑줄이 있어서 `AcmeCorp_SpecialCharge`를 `SpecialCharge`로 접으면 남의 계정과 합쳐지고 아래에서 구분할 방법이 없습니다. ④ **단위가 여덟 철자, 의미는 셋입니다** — `usd` 388,484 · `_usd` 36,280 · `usdollar` 3,128 · `usd/shares` 8,789 · `usd/share` 5,976 · `_usd_/_shares` 1,372 · `shares` 7,725 · `unit12` 7,185. `unit == "usd"`로 거르는 파생은 통화 값 39,408개를 버립니다. 그리고 주당 값을 **통화보다 먼저** 판정합니다 — `usd/shares`에는 `usd`가 들어 있고, 이걸 통화로 읽으면 EPS가 달러 금액이 되어 아래에서 진짜와 구별할 수 없습니다. `unit12`는 추측하지 않고 미분류로 이름 붙입니다.
- **답이 나왔습니다: 미국 10-Q 손익계산서는 회계연도 시작부터의 누계입니다 — 네 계정 전부, 표본 1,018~1,393 (2026-09-13).** 연간 패스가 돌아 공시 6,632건이 됐고 비율 측정이 판정했습니다 — 순이익 2.05·3.13, 매출 2.04·3.08, 영업이익 2.07·3.15, 영업현금흐름 1.96·3.12 (반기/1Q · 3Q/1Q). 누계가 예측하는 값은 2.0·3.0, 독립 분기는 1.0·1.0입니다. 네 계정 모두 첫 번째 밴드에 들었고 밴드 사이에 걸친 것은 없습니다. **따라서 TTM은 rollforward로만 만들 수 있습니다: `TTM(Y,Q) = FY(Y-1) − cum(Y-1,Q) + cum(Y,Q)`.** 네 분기를 더하는 방식 — 기억으로 쓴 파생이 골랐을 바로 그 방식 — 은 1분기를 네 번, 2분기를 세 번 세면서 FCF를 약 2.5배로 부풀리고, 그러면서 모든 숫자가 멀쩡해 보입니다. **같은 데이터가 제 코드의 결함 둘을 찾아냈습니다.** ① **10-K가 항상 1년은 아닙니다** — 1,661건 중 4건이 1년이 아닌 기간을 적고 있습니다(LYB 91일, TTWO 89일, DRI 244일, 그리고 기간을 아예 안 적은 1건). 회계연도 말을 옮길 때 내는 전환보고서입니다. 기간을 form에서 읽고 있었으므로 LYB의 91일 수치가 rollforward의 FY 항으로 들어가 그 해를 약 4분의 1로 만들 뻔했습니다. 이제 **기재된 기간 길이만** 보고, 네 단계 중 어디에도 안 맞으면 추측 대신 판정하지 않습니다. ② **값 5,353개가 단위가 아닌 라벨 아래 있습니다** — `unit12`·`unit1`·`u001` 같은 제출자 자신의 XBRL 단위 id이고, 그 아래 개념은 `NetIncomeLoss`·`Assets`처럼 평범합니다. 두 가지를 재고 결정했습니다: **같은 라벨이 공시마다 다른 뜻**이라(`unit1`이 한 공시에선 달러, 다른 공시에선 주식 수) 라벨→의미 표는 존재할 수 없고, **한 공시 안에서는 일관**됩니다(AMD 2012 10-Q는 달러 전부를 `unit1`, EPS 둘을 `unit14`에). 그래서 **공시 단위로** 앵커 개념에서 해석합니다 — `EarningsPerShareBasic`을 든 라벨이 그 공시의 주당 단위입니다. 실제 저장소에서 미분류 17,931개가 **358개**가 됐고(공시 259건), 원래 읽히던 라벨의 뜻이 바뀐 경우는 0건입니다. 여기에 가드 하나 — 앵커는 수량의 **종류**를 말할 뿐 **어느 통화**인지는 말하지 못합니다. `eur` 라벨 아래의 `Revenues`도 매출이지만 아래에서 `currency`는 달러를 뜻하므로, 통화로의 승격은 생성된 id 모양(6,632건에서 승격된 라벨은 전부 숫자를 포함했고, 이름 있는 단위 `pure`·`number`·`store`·`eur`는 전부 아니었습니다)으로 제한했습니다. 주당·주식 수는 통화가 없으므로 자유롭게 승격되고, 그래서 `eps` 철자 163개가 회수됩니다. 수집 진행률은 3,000 / 16,580창(18.1%)이고, **백필이 끝나기 전에는 리플레이에 연결하지 않습니다** — 유니버스의 18%로 만든 파생은 이름만 다른 생존 편향입니다.
- **미국 파생을 썼고, 애플 10-K 원문과 소수점 넷째 자리까지 맞습니다(2026-09-14).** `pipeline/finnhub_derive.py` + `build_pit_fundamentals.py --region us` → **PIT 행 33,832건, 779종목, 계약 수용 33,832 · 거절 0.** 검증은 자기 출력이 아니라 **원문**입니다 — 애플 FY2012(2012-09-29 종료, 2012-10-31 제출, 액면분할 전, 순이익 $41.733B·매출 $156.508B·영업이익 $55.241B·자본 $118.210B·부채 $57.854B·영업현금흐름 $50.856B·자본지출 $8.295B·희석주식수 945.355M)에서 일곱 팩터 전부 일치: roe 0.3530 · operatingMargin 0.3530 · profitMargin 0.2667 · debtToEquity 0.4894 · epsTtm 44.1453 · bookValuePerShare 125.0430 · fcfPerShare 45.0212. **시점 정확성도 로드된 스토어에서 확인**했습니다 — 2012-10-30에는 FY2012 숫자가 하나도 안 보이고, 제출일 10-31에 전부 보입니다. rollforward 산술도 같은 스토어에서 보입니다(2013 Q2까지 12개월 = 41.733 − 24.686 + 22.625 = 39.672B → epsTtm 41.91). **규칙과 각각이 거절하는 것:** TTM은 rollforward이고 네 단계를 더하면 1분기를 네 번 세서 FCF가 2.5배가 됩니다 · 직전 연도가 없으면 **연율화하지 않고 아무것도 내지 않습니다**(10%, 대부분 2012년) · 계정과목은 기억이 아니라 **공시 수를 세서** 고른 체인입니다(매출은 단일 태그 41.6% → 체인 93.7%) · 부채는 기재 68.5%지만 자산−자본으로 99.2% · **공시 이후의 것은 무엇도 참조하지 않습니다**(rollforward·주식수 이월·정정공시 전부 — 33,986개 중 286개가 두 번 제출됐고 **먼저 것을 남깁니다**, 정정이 이기면 정정이 존재하기 전에 끝난 창에 수정된 숫자가 들어갑니다) · 자본이 음수면 비율을 내지 않습니다(부실기업이 퀄리티 상위로 올라옵니다). 리플레이 구간 커버리지: roe 89.2% · profitMargin 88.7% · debtToEquity 93.9% · epsTtm 78.1% · fcfPerShare 69.7%. 퀄리티와 밸류를 따로 보고하는 이유는 따로 실패하기 때문입니다 — 밸류는 25.3%가 기재 안 하는 주식수가 필요합니다. **리플레이에는 아직 연결하지 않았습니다**: `replay.yml`은 여전히 `pit-kr.jsonl`만 넘기고, 미국을 붙이는 것은 봉인된 입력을 바꾸는 변경이라 새 `REPLAY_VERSION` 세대와 50분 재취득이 필요합니다 — 파생을 넣는 김에 딸려가는 게 아니라 그 자체로 결정해서 해야 하는 변경입니다.
- **봉인을 열었습니다 — `replay-v15`, 미국 밸류·퀄리티 슬리브가 13년 만에 처음 데이터를 갖습니다(2026-09-14).** `replay.yml`이 두 지역을 다 빌드해서 둘 다 넘기고, `DATA_VERSION`에 `finnhub-pit-fundamentals-us-v1`이 붙었습니다. **왜 연장이 아니라 새 세대인가:** v14까지 리플레이가 읽던 재무 파일은 `pit-kr.jsonl` 하나뿐이라 `fullComposite`가 미국 종목에서 관측치 0이었고 `claimEligible`이 false였습니다 — **실전 비중의 절반(value 0.3 + quality 0.2)이 13년 동안 미국 종목에서 한 번도 계산된 적이 없습니다.** v14의 미국 시그널은 전부 모멘텀과 나머지 슬리브만으로 만들어진 것이고, v15는 같은 이름들을 세 슬리브가 아니라 다섯 슬리브로 점수 냅니다. **입력이 다르면 횡단면 랭크는 비교 불가**이므로 v15를 v14에 이어붙이면 5슬리브 점수와 3슬리브 점수를 비교하게 됩니다. v14 기록은 봉인된 채로 둡니다. **리플레이가 이제 보게 되는 것**(합친 스토어에서 실측): 2013-06-28에 미국 **676종목**이 시점 재무 보유(roe 595 · epsTtm 484), 2018-06-29 726종목, 2025-06-30 778종목 — **v14에서는 전부 0이었습니다.** **두 지역은 따로 읽어서 합칩니다** — 벤더도 계정과목도 다르고 따로 실패하기 때문입니다. DART가 조용해지는 것과 finnhub가 조용해지는 것은 다른 사건인데 `rowsAccepted`가 하나면 한국 파싱 실패가 미국 커버리지 숫자로 읽힙니다. `perSource`가 파일별 기여를 싣고, 한 티커가 양쪽에 있으면 묻지 않고 보고합니다(한국 이름은 `.KS`/`.KQ`가 붙어 오늘은 일어날 수 없지만, 한 지역 이력이 다른 지역 이력을 조용히 덮는 건 밖에서 안 보입니다). **이걸로 고쳐지지 않는 것 하나, 그리고 그건 새 문제가 아닙니다:** 한국 스토어는 2015년에 시작하므로 KR 밸류·퀄리티는 2013~2014년에 v14와 똑같이 비어 있습니다. 미국은 이제 전 구간을 덮고 한국은 아직 아닙니다 — 두 세대 공통의 알려진 한계지 v15가 만든 게 아닙니다. **비용:** 첫 v15 실행이 전 입력을 재취득하고(약 50분) 13년 백테스트를 다시 계산합니다. v14 결과(ALPHA_RANK −4.87%p · CALIBRATED +1.08%p · pairwise CI [−1.006, +0.042] → INDISTINGUISHABLE)는 **미국 밸류·퀄리티가 비어 있는 상태에서 측정된 것**입니다. v15가 내놓는 숫자는 그 전략을 비중의 양쪽이 실제로 데이터를 든 채로 처음 재는 것이고, 옛 숫자의 정정이 아니라 다른 실험입니다.
- Validation Lab은 지역별 21/63/126/252일 Rank IC, bucket 단조성, rolling IC/spread, champion/challenger NAV, 예상-실제 괴리와 raw N·신호일·유효 독립일·HAC lag를 함께 표시합니다. Historical OOS와 prospective paper는 서로 다른 패널이며, 126일 prospective 결과가 없으면 `NOT_YET_MATURED`입니다.
- 과거 구성종목·PIT 재무·vintage macro 중 하나라도 없으면 integrity gate가 실패합니다. 이때 `SURVIVORSHIP_BIAS_UNRESOLVED`를 표시하고 Kelly 및 selector 승격은 금지됩니다. 과거 challenger가 좋아 보여도 production은 자동 교체되지 않습니다.
- **같은 코드로 두 번 돌렸더니 성적표가 달랐습니다 — 이게 지금 가장 심각한 결함입니다(2026-09-05).** 커밋 `38a4951` 하나로 같은 714개 리플레이 날짜를 두 번 돌렸는데, challenger CAGR이 **19.47% → 16.77%**, MDD가 **−13.35% → −9.85%**, 쌍대 차이가 **−0.702%p → −0.530%p**로 바뀌었습니다. 벤치마크 자신의 CAGR도 10.14% → 9.57%로 움직였고, 그게 "모델이 바뀐 게 아니다"를 확정해 준 단서였습니다.
- **원인은 상장폐지 종목 세 개입니다 — 추측이 아니라 원장 커밋 diff입니다.** 2013-01부터 2018-03까지 월별 신호 파일 **60개**가 재작성됐는데, **기존 행은 단 한 바이트도 안 바뀌었고** 행 **250개가 추가**됐습니다. 종목은 셋뿐입니다 — **`HAR` 215행, `MHP` 23행, `BMC` 12행**, 전부 미국 폐지종목입니다(HAR은 2017년 삼성 인수, MHP는 2013년 SPGI로 개명, BMC는 2013년 비상장 전환). 즉 **과거 단면의 구성원이 그 실행에서 가격 벤더가 응답했는지에 따라 달라집니다.** 생존편향과 별개 버그가 아니라 같은 뿌리입니다.
- **입력 0.2%가 평가 일정 84%를 흔듭니다.** **v6 당시** `block_dates`는 측정 가능한 날짜를 앞에서부터 greedy로 훑어 겹치지 않는 블록을 고르므로, 날짜 하나가 들어오거나 빠지면 그 뒤 블록이 전부 재고정됩니다. 두 실행의 롤링 평가지점 104개 중 **공유는 29개(16.2%)** 였습니다. 120,371행 중 250행 변화가 만든 결과입니다.
- **그래서 `replayDeterminism` 가드가 들어갔고, 어긋나면 보고서를 막습니다.** 원장은 매일 자라야 하므로 불변식은 "절대 안 변한다"가 아니라 **접두 안정성**입니다 — 이미 발표된 블록은 종료일까지 그대로여야 하고, 이미 발표된 날짜의 단면 구성도 그대로여야 하며, 마지막 기록 뒤에 붙는 것만 허용됩니다. 블록의 **종료일**까지 지문에 넣는 이유는 **v6 당시** `shared_block_dates`가 두 selector 중 **늦은** 종료일을 썼기 때문입니다(현재는 고정 달력): 진입일이 같아도 종료일이 밀리면 그 뒤 블록이 전부 밀리는데, 진입일만 보는 지문은 그걸 "안정"이라고 부릅니다. 어긋나면 `contractValidation`이 `replay_not_reproducible_past_changed`로 실패하고 `audit_portfolio.py`가 종료코드 1을 냅니다 — **과거가 움직인 위에서 계산된 성적표는 전략에 대한 증거가 아니기 때문입니다.** `replayVersion`이 바뀌면 새 실험이므로 위반이 아닙니다.
- **이 가드가 있었으면 그날 두 보고서 모두 막혔습니다.** 실제 원장 두 커밋으로 검증했습니다: 표본 3개월에서 드리프트 날짜 14개를 전부 잡아내고 `reproducible: False`를 냅니다.
- **`replay-v6`에서 challenger가 처음으로 통계적으로 분리됐고, 그래도 승격은 열리지 않습니다.** 132개 공통 블록에서 champion−challenger는 −0.684%p, 95% 부트스트랩 구간 [−1.127, −0.225]로 0을 포함하지 않습니다(champion 연환산 −0.578%, challenger +7.632%). v5에서 구분 불가였던 것이 KR 단면에 밸류·퀄리티가 들어오면서 갈라진 것입니다. 그러나 `integrityGate.failures`는 `historicalUniverse`·`vintageMacro`이고, `promotionEligible`은 false입니다. **분리는 승격 사유가 아니며, 이 숫자들은 생존편향이 미해결인 원장 위에서 계산된 값입니다.**
- **두 실패 중 `historicalUniverse`는 무료 데이터로는 닫히지 않습니다 — 산술이 그렇습니다.** `universe_ready`는 `constituentCoveragePct`와 `membershipCoveragePct`가 **둘 다 정확히 100.0**일 것을 요구합니다. 현재 미국은 편입이력 99.80%인데 가격 83.02%이고(=편입이력이 아는 종목의 17%를 벤더가 서빙하지 않음), 한국은 편입이력 0%에 가격 86.29%입니다. 한국 편입이력을 완벽히 만들어도 폐지종목 가격이 34.55%뿐이라 KR은 약 40%에서 멈추고, 미국의 17% 가격 결손은 그대로 남습니다. 즉 이 게이트는 데이터를 더 모아서 여는 게이트가 아니라, **현재 정의로는 도달 불가능한 게이트**입니다. 문턱을 낮추는 것은 답이 아닙니다 — 답이 있다면 결손을 0으로 만드는 것이 아니라 **결손이 결론을 뒤집을 수 있는지를 한계까지 측정하는 것**(빠진 종목에 최악값을 부여했을 때도 쌍대 차이가 부호를 유지하는가)이며, 그것은 아직 하지 않았습니다. 하지 않은 채로 유리한 쪽을 고르지 않았습니다.
- **KRX는 열렸습니다 — 한국 편입이력은 이제 미지수가 아니라 측정된 집합입니다(2026-09-05).** 승인 이후 `sto/stk_bydd_trd`(유가증권시장 일별매매정보)가 `AUTH_KEY` 헤더로 서빙됩니다. 판정 `POINT_IN_TIME`: 2013-01-02에 930종목, 2026-09-01에 943종목이고 **두 날짜의 겹침이 77.42%**(210종목 편출, 223종목 편입)입니다. 날짜 파라미터를 무시하고 오늘 명부를 되돌려주는 엔드포인트였다면 겹침이 100%였을 것이므로, 이 숫자가 곧 "날짜가 실제로 먹힌다"는 증거입니다. 행에는 `MKTCAP`과 `LIST_SHRS`가 함께 실려 있어 리플레이의 KR 유니버스를 **지수 편입명부 근사가 아니라 유니버스 자신의 규칙(시가총액)으로** 재구성할 수 있고, 그날 거래된 종목을 싣기 때문에 이후 폐지된 이름도 들어 있습니다.
- **오래된 날짜의 51/68은 결손이 아니라 상장 이력입니다 — 이것도 재서 확정합니다.** 리플레이의 KR 68종목 중 2013-01-02 단면이 담은 것은 51개(75.0%), 2016년 54개, 2020년 63개, 2026-09-01에 68개 전부입니다. 이 형태는 "빠졌다"가 아니라 "아직 상장 전"일 때 나오는 모양이지만, 그건 제 추측이지 측정이 아니었습니다. 그래서 `universe_reach()`가 각 이름의 **최초 등장 스냅샷**을 잡아 빠진 이름을 두 부류로 가릅니다 — `missingListedLater`(나중 날짜에는 있음 = 그때 상장 전, 단면이 옳고 고정 유니버스 쪽이 시대착오)와 `missingNeverHeld`(어느 날짜에도 없음 = 진짜 결손, 시장이 틀렸거나 코드가 틀렸거나). 둘은 정반대의 수리를 필요로 하므로 절대 합산하지 않습니다. 참고로 KOSDAQ(`sto/ksq_bydd_trd`)은 아직 401이지만 **리플레이 KR 68종목은 전부 `.KS`** 라 이 유니버스에는 걸리지 않습니다.
- **다만 KRX가 열려도 `historicalUniverse` 게이트는 열리지 않습니다.** `universe_ready`는 편입이력과 가격 커버리지가 **둘 다 100.0**이어야 하고, 한국은 폐지종목 가격이 34.55%, 미국은 가격 결손 17%가 그대로입니다. KRX가 바꾼 것은 게이트가 아니라 **한국 편입이력이 0%의 미지수에서 이름이 붙은 집합으로 바뀌었다**는 것이고, 편향 하한(`survivorshipBound`)은 바로 그런 집합 위에서만 세울 수 있습니다.
- **`vintageMacro`는 어떤 시작일에서도 닫히지 않습니다 — 재측정 결과입니다(2026-09-04, 2차 실행).** 파생 컬럼 결함은 실재했고 고쳤지만, 그것이 유일한 장애물이 아니었습니다. 상한을 안 건드리는 방식으로 다시 재보니 패널 28개 중 **10개는 ALFRED에 빈티지가 아예 없습니다** — `series/vintagedates`가 `"The series does not exist in ALFRED but may exist in FRED"`로 거절합니다. `Breakeven_10Y`·`Broad_Dollar`·`Core_PPI`·`HY_Spread`·`Headline_PPI`·`IG_Spread`·`Korea_10Y`·`Korea_3M`·`RRP`·`USD_KRW`입니다. FRED는 이 시리즈들을 서빙하지만 개정 이전 값은 보관하지 않습니다.
- **그래서 시작일을 옮겨도 소용없습니다 — 1차 실행 해석은 틀렸고 여기서 철회합니다.** 1차 실행의 `get_series_all_releases`가 이들에 대해 돌려준 `realtime_start`(2014·2019·2023 등)는 빈티지가 아니라 **FRED가 그 시리즈를 싣기 시작한 날짜**였습니다. 그걸 첫 빈티지로 읽고 "2018로 옮기면 6개가 살아난다"고 적었는데, 살아나지 않습니다. 빈티지가 없는 시리즈는 어떤 as-of 날짜에서도 없습니다.
- **상한 수정은 효과가 있었습니다.** 1차에서 죽었던 `Treasury_10Y`·`Treasury_2Y`·`FedFunds`·`Real_10Y`가 각각 첫 빈티지 2005-06-28·2005-06-28·2005-06-28·2005-10-12로 전부 `VINTAGE_USABLE`이 됐고, `Yield_Curve`도 두 입력이 살아나면서 함께 풀렸습니다. usable은 13개에서 **18개**로 늘었습니다. 남은 10개가 데이터의 답입니다.
- **따라서 두 integrity gate 모두 현재 정의로는 영구히 닫혀 있습니다.** `historicalUniverse`는 100.0/100.0을 무료 벤더가 못 채워서, `vintageMacro`는 패널의 10개 컬럼이 애초에 빈티지되지 않아서입니다. 남은 선택지는 두 개뿐이고 둘 다 사람이 결정할 일입니다: **패널에서 빈티지 없는 10개를 빼거나**(매크로 패널 정의 변경 = 모델 변경), **게이트가 요구하는 것을 바꾸거나**. 데이터를 더 모아서 여는 길은 없습니다.
- **부수 발견: `TGA`(`WTREGEN`)는 같은 id로 정의가 바뀌었습니다.** 2013 빈티지가 1,408개 기간을 서빙하는데 오늘의 시리즈는 같은 구간에 524개뿐입니다. 빈티지가 현재 시리즈보다 길 수는 없으므로 이건 id 재사용이며, 그대로 두면 건강한 컬럼으로 읽힙니다. `redefinedUnderSameId`로 표시합니다.
- **1차 실행에서 6개 시리즈는 프로브 자신의 버그로 못 쟀습니다.** `fredapi`의 `get_series_all_releases`는 전체 real-time 구간을 한 번에 요청하는데, 일별 시리즈는 ALFRED의 **빈티지 날짜 2,000개 상한**에 걸립니다(`DGS10` 5,103개). `NFCI`·`ANFCI`는 정확히 100,000행이 와서 행 상한에 잘렸습니다. 그래서 프로브는 `series/vintagedates`(첫 빈티지 날짜)와 `realtime_start=realtime_end=T`로 고정한 스냅샷(그날 그 컬럼이 실제로 서빙한 것)만 쓰도록 바꿨습니다 — 둘 다 상한에 걸리지 않습니다. 실패는 `"the ALFRED call did not return"`이 아니라 벤더가 말한 문장 그대로 기록합니다.
- **`historicalUniverse` 게이트가 100.0/100.0 요건에서 허용 임계값 20%로 바뀌었습니다(사람의 결정, 2026-09-17).** 무료 벤더로는 미국 가격 결손 17.30%, 한국 2.29%가 바닥이라 정확히 100이 영구히 닿지 않는 문턱이었다는 진단은 그대로였지만, 이번엔 "그러니 낮추지 말자"가 아니라 "이 정도는 감수하고 넘어가자"는 사람의 판단으로 결론이 바뀌었습니다. 근거 없이 숫자만 바꾼 게 아니라, 미국 194개 미가격 종목 전부가 다운로드 버그가 아니라 진짜 상장폐지임을 먼저 확인했고(`docs/us-delisted-prices-source.md`), `survivorshipBound`를 측정된 전체 갭(US 17.3%·KR 2.29%)까지 스트레스를 줘 champion-challenger 비교가 뒤집히지 않는 것(replay-v16: `NOTHING_TO_BOUND` —애초에 갈라진 결론이 없어 뒤집을 것도 없었고, 최악을 가정할수록 오히려 challenger가 더 유리해짐)을 확인한 뒤에 임계값을 도입했습니다. `pit_data.HISTORICAL_UNIVERSE_GAP_TOLERANCE_PCT`(20.0) 하나가 `UniverseHistory.snapshot`과 `historical_replay`의 풀링·지역별 리포트 양쪽의 유일한 기준이 되고, `affectedObservationsPct`는 게이트 통과 여부와 무관하게 실측값을 그대로 보고합니다(LOW라고 0%로 지워버리던 버그를 이 참에 고쳤습니다). 다만 `fullProductionFidelity` 플래그(운영과 완전히 동일하다는 더 강한 주장)는 `tolerance_pct=0.0`을 그대로 요구해 이 완화의 영향을 받지 않습니다. `survivorshipBound`는 폐지하지 않았습니다 — 임계값은 "게이트가 닿을 수 있는가"를 답하고, bound는 "이 특정 결과가 그 갭 때문일 수 있는가"를 매 실행마다 계속 답합니다.

## Alpha Calibration과 Kelly 기대수익 posterior

`alpha`·`rawAlpha`·`alphaPercentile`은 여전히 **기대수익률이 아닙니다.** 대신 과거 OOS ledger에 실증적으로 묻습니다: *"이 모델이 미래를 보지 않고 KR 90~95p에 넣었던 종목은 이후 126일 동안 KOSPI200 대비 실제로 어땠는가?"*

- **벤치마크 대비 초과수익은 모델의 알파가 아닙니다.** 버킷의 초과수익은 `유니버스 캐리 + 알파 스프레드`로 분해됩니다. 앞은 재현 유니버스 전체가 벤치마크 대비 낸 부분(구성·베타·기간 효과)이고, 뒤만 순위가 만든 것입니다. 합성 fixture에서도 이 캐리는 KR +2.4%p, US +6.0%p/126일로 커서, 분해하지 않으면 최상위 버킷 "알파"의 절반에서 전부가 캐리입니다. 그래서 기대수익으로는 **level과 스프레드 중 작은 쪽**을 쓰고, 어느 쪽이 구속했는지(`conservativeBasis`)를 함께 기록합니다. 유니버스가 벤치마크를 이긴 통상적인 경우에는 스프레드가 구속해 캐리가 제거되고, 반대로 **유니버스가 벤치마크에 뒤진 경우에는 level이 구속**합니다 — 떨어지는 유니버스 위에 있다는 것만으로는 실제 벤치마크 대비 수익이 0일 수 있고, 그 스프레드를 실력으로 계상하면 캐리를 계상하는 것과 같은 오류가 방향만 바뀐 것입니다.
- **순위 정렬 증거가 없으면 사다리를 발표하지 않습니다.** isotonic 회귀는 어떤 입력에도 단조 결과를 만들기 때문에, 정렬 증거 없이 적용하면 잡음이 깔끔한 사다리로 세탁됩니다. 승인 조건은 **실제로 발표될 보수적 수치가 스스로 유의할 것**입니다: 최상위 버킷 자체의 스프레드가 `minOrderingTStat`(기본 1.96)를 넘어야 하고, 여기에 최상위−최하위 또는 날짜별 Rank IC 중 하나가 뒷받침해야 합니다. 최하위가 무너져서 생긴 스프레드는 롱온리 북이 걷을 수 없으므로 `TOP_BUCKET_SPREAD_NOT_SIGNIFICANT`로 거절합니다. 미달이면 `ORDERING_NOT_ESTABLISHED`로 표시하고 Kelly 기대수익에 쓰지 않습니다.
- **통과했다는 사실 하나로 뭉뚱그리지 않습니다.** Rank IC가 유의하면 `FULL_RANK`(전 구간 정렬 확인), 최상위 스프레드만 유의하면 `EXTREMES_ONLY`(최상위 구간만 유의)입니다. 최상위만 날카롭고 중간은 거꾸로 가는 점수는 실제로 흔하며, 집중 포트폴리오는 최상위에서만 담으므로 기대수익 산정에는 쓸 수 있지만 사다리 전체의 근거로 제시하지 않습니다. IC의 부호로 거절하지도 않습니다 — 쓸 수 있는 엣지를 버리는 일이기 때문입니다. 이 1.96은 표에서 가져온 값이 아니라 이 gate 자체를 귀무가설 하에서 시뮬레이션해 실제 기각률이 3.6~5.0%가 되도록 맞춘 값입니다.
- 같은 날 수백 종목은 **날짜 단위로 군집화**하고, 겹치는 126일 forward return은 Bartlett kernel Newey–West/HAC로 보정합니다. **HAC lag은 재현 그리드 간격에서 환산합니다** — 126세션 중첩은 일간 그리드에서 관측치 126개지만 주간 그리드에서는 약 26개입니다. lag을 거래일 수(125)로 고정하면 주간 계열에서 존재하지 않는 125개 시차를 추정하게 되고, 그 잡음은 장기분산을 아래로만 밀어 t와 유효표본을 부풀립니다(독립 주간 관측치에서 명목 5% 검정의 실제 기각률이 13%, 명목 10%가 20~45%였습니다). 낙폭·CVaR 같은 **경로 의존 지표는 겹치지 않는 표본에서만** 계산합니다(겹친 수익률을 이어 붙이면 존재하지 않는 낙폭이 만들어집니다).
- 표본이 얇으면 `eff/(eff+prior)`로 0을 향해 수축합니다. 수축과 표준오차는 **level이 아니라 스프레드 계열의 유효표본**에 붙습니다 — 유니버스가 함께 움직여서 정밀해 보이는 level은 순위에 대해 아무것도 말해주지 않기 때문입니다.
- **과거 증거 할인율**: 기본 `0.6`(0.5~0.8 구간의 중간). 과거 재현은 시점상 진짜 out-of-sample이지만, **모델 설계 자체가 그 시기를 겪은 뒤에 만들어졌다**는 점은 어떤 replay로도 제거할 수 없습니다. 실시간 증거(1.0)보다 낮되, 비어 있는 실시간 ledger를 이기기에는 충분한 값입니다. 여기에 PIT 커버리지 부족·생존편향·매크로 개정·재무 근사·표본 부족·국면 집중·섹터 집중·fold 불안정이 있으면 각각 추가 할인됩니다.
- **결합은 정밀도 가중**입니다: `mu_post = Σ(w_i/se_i² · mu_i) / (Σ w_i/se_i² + 1/τ²)`. 0을 중심으로 하는 사전분포(τ=3%)가 얇은 추정치를 수축시킵니다. 실시간 표본이 쌓일수록 정밀도가 커져 **자동으로** 실시간 비중이 올라갑니다 — 전환 시점을 하드코딩하지 않습니다.
- **비대칭이 의도적입니다.** 실시간 성과가 과거보다 크게 낮으면 Kelly fraction을 축소하고, 반대 방향은 작동하지 않습니다. 실시간 성과가 나쁘다고 기대수익을 올리는 일은 절대 없습니다.
- Kelly 활성화 상태는 단일 코드가 아니라 **증거 사다리**입니다: `NO_EVIDENCE` → `HISTORICAL_PRIOR_ONLY` → `HISTORICAL_OOS_SUPPORTED` → `PROSPECTIVE_EARLY` → `PROSPECTIVE_CONFIRMED` → `ACTIVE_PAPER` → `ACTIVE_VALIDATED`. 화면에는 "과거 OOS 검증 활용 중", "실시간 검증 축적 중"처럼 표시합니다.

## 상승확률·상하방 분포 calibration

켈리 비중은 평균 기대수익 하나만 맞는다고 안전해지지 않습니다. 그래서 126일 비용 차감 지역 초과수익을 `상승(>0) / 하락(≤0)`으로 나눠 **상승확률, 평균 상승폭, 평균 하락폭, 손익비, 이진 Kelly 참고값**을 별도로 발표합니다. 이진 Kelly 값은 설명용이며 실제 포트폴리오 비중은 상관관계를 포함한 다변량 최적화가 결정합니다.

- **만기 전 정답 사용 금지:** 1월 2일 신호의 126일 결과는 그 `endDate`가 지난 뒤에만 expanding 학습 상태에 들어갑니다. 신호일이 과거라는 이유로 아직 알 수 없던 미래 결과를 학습시키지 않습니다.
- **시점외 확률 감사:** 앞쪽 구간에서 `BASE(지역×알파버킷)`와 `MACRO(거시 국면 조건부)`, `ENTRY(종목 진입상태 조건부)` 세 가지만 비교하고, 뒤쪽 감사 구간의 Brier score·ECE·log-loss·지역 기준 대비 Brier skill을 별도로 평가합니다.
- **조건부 과최적화 제한:** 거시·진입상태 조건은 앞쪽 구간에서 최소 개선폭을 넘은 경우에만 후보가 되며, 뒤쪽 감사까지 통과하고 해당 셀의 유효 날짜가 충분해야 현재 종목 확률에 적용됩니다. 아니면 자동으로 지역×알파버킷 기본값으로 돌아갑니다.
- **한 날짜는 한 군집:** 같은 날 수백 종목의 승패를 수백 개 독립 표본으로 세지 않습니다. 확률 손실과 payoff는 날짜 안에서 먼저 평균합니다.
- **얇은 셀 수축:** Beta-Binomial 사전분포와 payoff prior가 얇은 조건부 확률·상승폭·하락폭을 부모 구간으로 수축합니다. 3개 사례의 100% 승률 같은 값이 비중을 지배하지 못합니다.
- **Brier skill 기준은 음수가 아닙니다.** `minBrierSkill`은 0 이상이어야 합니다 — 지역 기저확률을 그대로 내놓는 상수 예측기는 Brier가 구조적으로 0.25 이하, ECE≈0, log-loss≈0.69라서 `maxBrier`·`maxEce`·`maxLogLoss`를 **항상** 통과합니다. 즉 정보량이 0인 분포를 걸러낼 수 있는 검사는 skill 하나뿐이며, 이 분포가 Kelly 비중을 정합니다. 음수 기준은 잡음 허용치가 아니었습니다 — 귀무가설을 시뮬레이션하면 skill의 표준편차가 약 0.001이라, 기존 −0.01은 약 9 표준편차의 여유였고 정보 없는 모델을 100% 통과시켰습니다.
- **Fail closed:** `probabilityGate.requiredForKelly=true`입니다. 어느 보유 지역이라도 확률 감사에 실패하거나 현재 종목의 분포를 찾지 못하면 `probability_calibration_unreliable`로 기록하고 Kelly 종목 비중을 숨긴 채 컨빅션 위험가중을 유지합니다.
- **운영모델 동일성도 별도 gate:** Brier가 좋아도 현재 상장종목만 과거로 보낸 재현, 발표일 없는 재무, 개정된 매크로를 운영 모델 전체의 검증으로 인정하지 않습니다. 기본 설정은 과거 구성종목과 PIT 재무를 요구하고, 거시 조건부 확률을 쓸 때는 vintage 거시도 요구합니다. 지금 무료 데이터에 이 세 가지가 없으면 확률 성능과 무관하게 Kelly는 미적용이 정상 동작입니다.

## Opportunity · Warning Radar

Core Portfolio는 "위험 대비 지금 안정적으로 보유할 3~5종목"을 답합니다. 레이더는 **다른 질문**에 답합니다: *"최근 무엇이 달라졌는가?"* 두 층은 **하나의 점수로 합치지 않습니다.**

- 그래서 **변화(change) 피처**가 수준(level) 피처만큼 중요합니다: 알파 백분위 Δ, 20/60일 모멘텀 가속, 상대강도 Δ, 거래량 가속, 진입상태 전환, 섹터 로테이션 전환, 변동성 국면 전환, 200일선 돌파/이탈. **2년 내내 95p였던 종목은 기회가 아닙니다 — 아무 일도 일어나지 않았습니다.**
- ML은 복잡성보다 검증 가능성을 우선합니다. 로지스틱 baseline이 항상 후보에 있고, **OOS에서 baseline을 못 이기면 채택하지 않습니다.** target도 4종(양의 초과수익 / +5% 초과 / 횡단면 상위 20% / 비대칭 MFE-MAE)을 validation 안에서 비교하며, 사후에 가장 예뻐 보이는 것을 고르지 않습니다.
- **합격 판정은 알파 calibration과 같은 귀속 규칙을 따릅니다.** `netExcessSurvivesCost`·`beatsBaselineOutOfSample`·`holdoutHolds`는 벤치마크 대비 원시 초과수익이 아니라 **날짜별 유니버스 대비 기여분**(level과 스프레드 중 보수적인 쪽)으로 판정합니다. 절대 기준을 level로 맞추면 유니버스 캐리로 통과합니다 — 운영 ledger에서 무작위 선택의 상위 10%가 126일당 +1.1~2.4%를 벌어 0.5% 기준을 실력 없이 넘습니다. 산정할 수 없었던 검사는 `true`가 아니라 `None`으로 기록해 통과처럼 보이지 않게 합니다.
- 평가는 정확도가 아니라 **점수 구간별 실제 미래 초과수익의 단조성**과 상위 구간 실측 초과수익이 결정합니다. AUC·PR-AUC·Brier·calibration error·precision@K·turnover·MDD·CVaR는 맥락으로 함께 봅니다.
- **과최적화 방지**: nested walk-forward(선택은 validation에서만), purge+embargo, 시도한 variant 수와 grid 크기 기록, fold별 안정성, date-block bootstrap, Deflated Sharpe Ratio, PBO 추정, 그리고 **모델 확정 전까지 손대지 않는 최종 홀드아웃**(기본 2023~). 홀드아웃은 봉인 상태에서 접근하면 예외를 던집니다.
- **실패 조건을 명시적으로 구현합니다**(§37). baseline 미달·단조성 없음·비용 후 edge 소멸·fold 불안정·PIT 커버리지 부족·국면/섹터 집중·홀드아웃 붕괴 중 하나라도 걸리면 **모델을 적용하지 않고** 투명한 규칙 기반 변화 점수를 유지하며, 화면에 그 사실을 표시합니다.
- 3단계 등급: **검증된 기회**(과거 OOS 검증 통과 + 신호 수렴), **관찰 필요한 변화**(변화는 강하나 검증 부족 — 리서치 전용), **관심 목록**. 검증 모델이 없으면 "검증된 기회"는 **구조적으로 도달할 수 없습니다**(validator가 강제).
- **거래량 급증 + 가격 하락은 기회로 분류하지 않습니다.** 분배(distribution)이지 매집이 아닙니다.
- Warning은 기회 점수의 반대값이 아니라 **별도 점수·별도 모델**입니다. 변동성 급등처럼 기회 쪽에 짝이 없는 항이 있고, 조용한 종목이 조용하다는 이유로 위험해 보이면 안 되기 때문입니다. **보유 종목의 경고는 최상단**에 배치합니다.
- 오늘의 변화 알림은 **state transition 기반**입니다. 어제부터 HIGH였다면 오늘 다시 신규 알림을 만들지 않습니다.
- **과거 유사 사례**(historical analogue)는 설명·리서치 도구이며 주 예측모델이 아닙니다. 스케일링 후 같은 지역 안에서만 거리를 계산하고, 표본이 얇으면 수치를 노출하지 않습니다.

## 제약형 Fractional Kelly 모델 포트폴리오 (shadow)

위 컨빅션 기준 비중을 기본값으로 유지하면서, 검증된 paper ledger가 충분할 때만 제약형 포트폴리오 Kelly를 25% 혼합합니다. 기본식은 `75% × 컨빅션 기준 비중 + 25% × 제약형 Kelly 비중`이며, Full Kelly가 아니라 기본 0.25 Fractional Kelly를 사용합니다. Kelly가 켜져도 보유 종목 집합은 선정 단계가 정한 3~5종목 그대로이고, 바뀌는 것은 그 안의 비중입니다.

- 현재의 `alpha`, `rawAlpha`, `alphaPercentile`은 기대수익률이 아닙니다. 과거 신호 시점에 고정된 percentile 구간과 만기된 126영업일 OOS 초과수익을 연결한 뒤, **유니버스 캐리를 날짜별로 차감하고**, 거래비용을 차감하고 유효표본 수에 따라 0 방향으로 수축합니다. 같은 날 종목 횡단면은 한 날짜로 군집화하고, 겹치는 126일 forward return은 Bartlett kernel Newey–West/HAC로 보정하되 lag은 재현 그리드 간격에서 환산합니다. alpha bucket 단조성 복원(weighted isotonic)은 **순위 정렬 증거가 확인된 지역에서만** 적용하고 경고를 남깁니다.
- 기대수익과 위험의 단위를 맞추기 위해 **KR·US를 별도로 최적화**합니다. 기대수익은 해당 지역 벤치마크 대비 초과수익, 공분산은 `종목 일수익률 - 지역 벤치마크 일수익률`의 regional active covariance입니다. 지역별 결과는 기존 위험예산과 지역 상한으로 결합하며, KR·US 절대수익률을 한 행렬에 섞지 않습니다.
- 표본이 부족하거나 Ledoit–Wolf 공분산을 계산할 수 없으면 기대수익이나 상관관계를 임의로 만들지 않습니다. 상태를 `SHADOW_INSUFFICIENT_HISTORY`로 기록하고 컨빅션 기준 비중만 유지합니다. 다만 **지역 active covariance는 Kelly와 무관하게 추정**하므로 보유 포트폴리오의 tracking error는 그때도 표시됩니다.
- 종목 30%, 업종 45%, 테마 45%, 지역 상한, 최소 현금 15%(국면·매크로 예산에 따라 상승), 최대 회전율 25%를 적용합니다. `EVENT_RISK`·`AVOID`·value trap은 신규 Kelly 비중 0, `WATCH`와 `WAIT_FOR_PULLBACK`은 각각 haircut을 받습니다. 제약 때문에 남은 금액은 다른 종목에 억지로 배분하지 않고 현금으로 둡니다.
- 매크로는 종목 기대수익에 더하지 않고 Kelly fraction과 최소 현금만 조정합니다. 낮은 confidence는 Kelly 위험예산을 추가 축소합니다.
- 거시와 개별 종목 환경은 두 단계로 반영합니다. 거시는 전체 Kelly fraction·현금 하한을 먼저 조정하고, 종목 진입상태는 개별 상한을 감쇠합니다. 별도의 조건부 상승확률은 시점외 Brier 감사에서 실제 개선이 확인된 경우에만 사용하며, 그렇지 않으면 환경을 확률에 억지로 더하지 않습니다.
- 거래비용은 지역별 수수료·세금·스프레드, 예상 회전율, 리밸런싱 주기, 예상 거래대금을 구조화해 계산합니다. 종목별 유동성 자료가 없으면 config의 보수적 기본 스프레드를 사용하고 fallback 여부를 기록합니다.
- 기준통화 기본값은 KRW입니다. 현재 종목 active covariance에는 환율 수익률과 환헤지를 넣지 않으므로 `currencyPolicy`와 화면에 이 한계를 명시하고 환율 위험은 지역 배분 레이어에서만 표시합니다.
- 대시보드는 **Base Kelly fraction**, **매크로 조정 applied fraction**, **최종 blend weight**를 구분합니다. 실제 영향도는 최종 비중과 기준 비중(현금 포함)의 절대 배분 차이인 `kellyAllocationImpactPct`로 표시합니다. fallback이면 Kelly 종목 비중을 공개하지 않습니다.
- 이전 운영 상태가 없으면 회전율을 실제 교체율처럼 보이게 두지 않고 `turnoverBasis=INITIAL_BUILD`로 기록해 화면에 **신규 구축**으로 표시합니다.

Kelly Criterion은 추정된 성장률을 최대화하는 도구일 뿐 만능 공식이 아닙니다. 확률·기대수익·공분산 추정오차에 민감하고, 과거 분포가 미래에도 유지된다는 보장이 없으며, Full Kelly는 큰 낙폭을 만들 수 있습니다. 이 모델은 개인의 자산·소득·부채·투자기간·손실감내도를 반영하지 않는 연구용 가상 포트폴리오입니다.

## 실행

```bash
pip install -r requirements.txt
python -m compileall pipeline
pytest -q
python -m pipeline.build                       # 실데이터 (Yahoo/FRED 네트워크 + FRED_API_KEY 필요)
python -m pipeline.validate data/site-data.json
# 오프라인 미리보기(합성 데이터; 생성물은 Git에서 무시됨):
python scripts/make_seed.py
python -m pipeline.validate data/site-data.json --allow-seed

# 과거 point-in-time 재현 (증분 — 이미 있는 날짜는 다시 계산하지 않음)
python scripts/run_replay.py <ledger-dir> [--start 2013-01-01] [--frequency W] [--full]
python scripts/train_opportunity.py <ledger-dir>            # 기회 모델 학습·검증
python scripts/train_opportunity.py <ledger-dir> --warning  # 경고 모델 학습·검증

# 합성 데이터로 전체 체인 점검 (기계 검증용 — 투자 결과 아님)
python scripts/demo_replay.py --tickers 40 --years 9
```

GitHub Actions의 **Historical point-in-time replay**를 수동 실행할 때 일반 검증은
기본값 그대로 `full=false`, `frozen_inputs=true`, `confirm_new_generation=false`,
`retrain=false`를 사용합니다. 이 모드는 커밋된 frozen input만 읽습니다. 벤더에서 새 입력을
받는 작업은 단순 검증이 아니라 새 replay 세대 생성입니다. `DATA_VERSION`과
`REPLAY_VERSION`을 먼저 의도적으로 올린 뒤에만 `full=true`, `frozen_inputs=false`,
`confirm_new_generation=true`를 함께 선택해야 하며, 그렇지 않으면 workflow가 비싼 작업을
시작하기 전에 중단합니다.

빌드는 다음 환경변수로 과거 ledger를 읽습니다(CI가 `signal-history` 브랜치에서 주입): `HISTORICAL_SIGNALS_PATH`, `HISTORICAL_OUTCOMES_PATH`, `HISTORICAL_DIAGNOSTICS_PATH`, `OPPORTUNITY_MODEL_PATH`, `WARNING_MODEL_PATH`. 없으면 과거 prior 없이 정상 동작하며 그 상태를 artifact에 기록합니다.

**계산비용.** 전체 재현은 종목수 × 재현 날짜 수에 비례합니다(주 단위 기준 550종목 × 13년 ≈ 수십 분). 그래서 `.github/workflows/replay.yml`은 **증분**으로 돌고, ML 재학습은 주 1회입니다. 일주일치 새 관측치가 walk-forward 선택을 바꿀 수 없는데 매일 재학습하면 비용만 늘고 **유효 시도 횟수만 부풀립니다**. 재현 주기 선택(`D`/`W`/`M`)의 trade-off는 `config.historicalReplay._notes`에 기록했습니다: 일 단위는 5배 비용에 대부분 겹치는 관측치만 추가되고 HAC 유효표본은 거의 늘지 않으며, 월 단위는 버킷 calibration에 필요한 횡단면 수를 밑돕니다.

`data/site-data.json`과 `data/audit.json`은 workflow/로컬 명령이 만드는 생성물이며 Git에서 추적하지 않습니다. 테스트용 최소 예시는 `tests/fixtures/site-data.synthetic.json`에 있고 파일명과 내부 `dataMode` 모두 synthetic임을 명시합니다. regional active Kelly·universe diagnostics·run-to-run diff가 추가된 스키마는 `2.3.0`이었고, `2.4.0`에서 과거/실시간 증거 분리와 레이더, `2.5.0`에서 만기 인지형 확률 감사, `2.6.0`에서 alpha·portfolio·integrity 진단이 추가되었습니다. **현재 스키마는 `2.7.0`**이며 일별 세션 정규화, 지역별 벤치마크 커버리지 gate, 검증 원장 commit provenance를 추가합니다. 기존 필드는 그대로 유지되므로 이전 소비자는 새 섹션을 무시하면 계속 동작합니다.

`FRED_API_KEY`(및 KR 매크로용 `ECOS_API_KEY`)는 GitHub Actions Secret으로만 주입합니다. 네트워크가 막힌 환경에서는 실데이터 빌드가 불가능하며, 그 경우 seed로 성공한 척하지 않고 차단 원인을 보고합니다.

## 실전(liveValidated) 활성화 조건

최소 1년(252영업일) paper tracking과 공통 표본 gate, 시간순 신호 축적, 비용 차감 후 양의 rank IC/초과수익, 벤치마크 대비 개선, 허용 가능한 MDD/CVaR, 섹터·기간 집중도 완화가 확인되기 전까지 `liveValidated`는 부여되지 않습니다.
